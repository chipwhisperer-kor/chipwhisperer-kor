# 후보 변환 산출 (`kit/candidates/`)

단계 **2b·3B** 작업 디렉터리. 정규 `Papers_md`·카탈로그를 대체하지 않는다.

```text
candidates/<rel>/
  CROSSCHECK.md
  <tool_id>/extract.md
  <tool_id>/META.json
```

`<rel>` = source의 `Papers_pdf/` 상대 키 (중첩 참고문헌 포함).

명령: 루트 `README.md`, `kit/PDF_TO_MARKDOWN.md`. allowlist: `../TOOL_ALLOWLIST.md`.
