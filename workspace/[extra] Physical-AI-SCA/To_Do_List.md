# To_Do_List — 남은 작업

이 문서는 **환경 구축이 끝난 시점(2026-08-10)에 남아 있는 일**을 적는다.
배경과 설계는 `README.md`, 사이클 절차는 `PROMPT.md`, 행동 규칙은 `AGENTS.md` 에 있다.

각 항목에는 **왜 필요한지**와 **무엇을 보면 끝났다고 할 수 있는지(완료 판정)** 를 적었다.
끝난 항목은 지우지 말고 완료 판정 근거를 함께 남긴다 — 다음 사람이 "정말 했는가" 를
다시 확인할 수 있어야 한다.

---

## 0. 지금 상태 — 무엇이 끝났나

계획한 11단계는 **전부 수행했고 실행으로 검증했다.** 아래는 그 결과다.

| 확인한 것 | 근거 |
|---|---|
| 에뮬레이션 수집·분석·보고·검증 전 단계 동작 | `runs/{001,002,003}/` 에 보고서 3종 + 증거 번들, `verify` 전부 통과 |
| 검출기가 실제로 반응한다 | 비마스킹 대조군(001)에서 결함 후보 790개, `AddRoundKey`·`SubBytes` 를 소스 행까지 지목 |
| 마스킹이 공격을 막는다 | 공격자 관점 CPA — 비마스킹 16/16 복구 vs 마스킹 1/16(평균 순위 132.9 ≈ 무작위) |
| 실측 채널에서도 파이프라인이 돈다 | 003 — 기존 SCHEMA 1.0 h5 로 DPA·CPA 수행, CPA 16/16 |
| 못 지킨 요건이 감춰지지 않는다 | 대조표가 001·002 에서 미준수 2 · 미기록 3, 003 에서 미준수 2 · 미기록 12 를 그대로 보고 |

**그러나 이 환경은 사전 진단이지 적합성 평가가 아니다.** 구조적으로 채울 수 없는 항목
(독립 시험소·승인 기관·육안 검사·ISO/IEC 19790 적합성)은 `범위밖` 으로 남으며 아래 목록에
넣지 않았다 — 그것은 할 일이 아니라 이 환경의 경계다(`README.md` §2).

---

## A. 실물 하드웨어가 있어야 하는 것

현재 실물 CW 환경이 구성되어 있지 않아 **한 번도 실행되지 않은** 영역이다.
코드가 맞아 보인다는 이유로 동작한다고 적지 않았다.

### A1. `cw_power.py` 구현·검증 — 실물 전력 수집기

| | |
|---|---|
| 현재 | 골격만. 호출하면 `NotImplementedError` 와 함께 만드는 순서를 안내한다 |
| 왜 | 세 관측 중 **물리 층**을 보는 유일한 도구다. 에뮬레이션의 HW/HD 모델은 글리치·커플링을 담지 않으므로 여기서 깨끗해도 실물에서 샐 수 있다 |
| 하는 법 | `physai/collectors/cw_power.py` 머리말의 5단계. 장비 제어는 `[extra] SCALib/dataset_collect_lib.py` 의 검증된 코드를 그대로 쓴다(12만 장 수집에서 자동 복구 3회 성공 이력) |
| 완료 판정 | 소량(수십 장) 수집에서 **골든 AES 일치**, 이어서 spec 대로 수집한 h5 가 `validate_dataset` 위반 0건 |

### A2. `cw_debugtrace.py` 구현·검증 — 타이밍 분석의 본래 계측기

| | |
|---|---|
| 현재 | 스키마(`SCHEMA.md` §6.6)와 골격만 |
| 왜 | **에뮬 TA 는 명령어 수를 셀 뿐 사이클을 재지 못한다.** 명령어 수가 같아도 constant-time 이 증명되지 않는다(Cortex-M4 는 명령어마다 사이클이 다르고 플래시 wait state 도 있다). 그 확정은 이 채널의 몫이다 |
| 하는 법 | `1.0.TraceWhisperer_main.ipynb` 의 SWO 설정 절차 → `set_isync_matches(진입, 복귀)` → 타임스탬프 차를 `exec_time` 으로. 전력 파일과 **분리 저장**(`SCHEMA.md` §2) |
| 완료 판정 | `exec_time_unit="trace_tick"` 과 `exec_time_epsilon` 이 기록된 h5 가 나오고, 같은 입력의 TA 결과가 에뮬 결론과 **일치하거나 어긋나는지**가 보고서에 실린다 |

### A3. A.2.5 전처리 — 같은 입력 10회 평균

| | |
|---|---|
| 현재 | `preprocessing_average_n=1`. 대조표에서 **미준수** (001·002·003 공통) |
| 왜 | ISO/IEC 17825 A.2.5 `shall [A.01]` 이 Level 3 에서 10회 실행의 평균을 트레이스 1장으로 삼으라고 요구한다 |
| 비용 | **수집 시간이 10배**가 된다. Level 3 을 주장하려면 치러야 하는 값이다 |
| 완료 판정 | 대조표의 `A.2.5` 행이 `준수` 로 바뀐다 |

> 에뮬레이션은 결정적이라 평균이 값을 바꾸지 않는다. 그래도 **요건을 충족한 것은
> 아니므로** 대조표는 계속 미준수로 보고한다. 이 항목은 실물 채널의 일이다.

### A4. `bandwidth_hz` 측정·기록 — Annex B `[B.01]`·`[B.02]`

| | |
|---|---|
| 현재 | 기록 없음 → 대조표 **미기록**, 요건을 **판정할 수 없다** |
| 왜 | B.01 은 대역폭 ≥ 클럭의 50 %(SW), B.02 는 대역폭의 5배 샘플링을 요구한다. 대역폭을 모르면 데이터가 좋고 나쁨을 떠나 **판정 자체가 불가능**하다 |
| 주의 | 값을 모른 채 추정해 채우지 않는다(`SCHEMA.md` §5.3). 미기록이 거짓 기록보다 낫다 |
| 완료 판정 | h5 루트 attrs 에 `bandwidth_hz` 가 있고 대조표의 B.01·B.02 가 준수/미준수로 **판정된다**(둘 중 무엇이든 판정되면 성공이다) |

### A5. `shunt_ohm` + 선택 근거 — Annex B `[B.05]`·`[B.06]`

| | |
|---|---|
| 현재 | 프로브 구성 문자열만 있고 저항값·선택 절차 없음 → **미기록** |
| 왜 | B.06 은 "**IUT 가 동작하는 범위에서 가장 큰** 저항" 을 고르라고 한다. 값만 적어서는 그 절차를 밟았는지 알 수 없어 `shunt_selection_note` 를 함께 요구한다 |
| 하는 법 | 저항을 바꿔 가며 타겟이 정상 동작하는 최대값을 찾고, 그 과정을 근거 문자열로 남긴다 |
| 완료 판정 | `shunt_ohm`·`shunt_selection_note` 가 h5 에 있고 대조표가 판정한다 |

### A6. 레코드별 `exec_time` 수집 — A.2.4 `shall collect`

| | |
|---|---|
| 현재 | 실측 h5(003)에 없음 → TA **수행 불가**, 대조표 미기록 |
| 왜 | Annex A 전체에서 `shall collect` 는 A.2.4(타이밍 측정) **하나뿐**이다. 가장 강한 데이터 수집 요구인데 빠져 있다 |
| 함정 | **분석 때 만들 수 없다.** 파형만 저장하고 이 값을 빠뜨리면 사후 산출이 불가능하다 — 트리거 구간의 원래 길이가 이미 사라졌기 때문이다 |
| 하는 법 | 캡처마다 `scope.adc.trig_count` 를 `exec_time` 배열에 남긴다. `exec_time_unit="adc_sample"`, `exec_time_epsilon` = 타겟 클럭 1사이클에 해당하는 샘플 수 |
| 완료 판정 | 실측 데이터셋으로 `analyze` 를 돌렸을 때 TA 가 `not-run` 이 아니라 판정을 낸다 |

### A7. SPA 파형쌍 subset 수집 — A.2.2

| | |
|---|---|
| 현재 | 실측 h5에 `simple-analysis` role subset 없음 → SPA **수행 불가** |
| 왜 | §7.3.2 `shall [07.03]`·§8.1 `shall [08.01]` 이 TA·SPA·DPA **셋 모두**를 요구한다. 하나라도 빠지면 필수 요건 미충족이다 |
| 하는 법 | Level 3 은 11 파형. `same-data`(고정/고정) · `different-data-fixed`(**키 랜덤**/평문 고정) · `different-data-random`(키 고정/평문 랜덤) 세 종류를 `spa_pair_kind` 로 구분해 수집 |
| 완료 판정 | 대조표의 `A.2.2` 행이 판정되고, `spa_traces.svg` 가 증거 번들에 나온다 |

### A8. `workspace/lib/cw_bench.py` 승격

| | |
|---|---|
| 현재 | 벤치 층(약 400줄)이 `[extra] SCALib/dataset_collect_lib.py` 안에 있고, `cw_power.py` 가 경로 삽입으로 import 하도록 되어 있다 |
| 왜 미뤘나 | `Bench._reopen()` 이 SCALib 전용 `_seed_masks()` 를 직접 호출하므로(`dataset_collect_lib.py:845`) 중립 모듈이 되려면 `on_reconnect` 콜백 인자를 더하는 **로직 변경**이 필요하다. 실장비가 없어 그 변경을 검증할 수 없었고, **검증 없는 하드웨어 리팩터링은 조용히 수천 장을 망가뜨린다** |
| 하는 법 | A1 이 실기로 동작한 **뒤에** 콜백 인자를 추가하고, SCALib 수집 노트북과 새 수집기 양쪽에서 수집을 다시 돌려 확인 |
| 완료 판정 | 두 프로젝트가 같은 `cw_bench` 를 쓰고, 양쪽 모두 소량 수집에서 골든 일치 |

### A9. 에뮬레이션 ↔ 실측 교차 검증 ★

| | |
|---|---|
| 왜 | **이 환경을 만든 이유 그 자체다.** 같은 `aes.c` 를 같은 플래그(`-Os`)로 컴파일했으므로, 에뮬에서 지목한 지점이 실측에서도 보이는지 확인해야 "가장 약한 고리" 를 말할 수 있다 |
| 현재 근거 | 간접 증거만 있다 — 실측 h5 Trace 길이 비율(masked/normal) **1.6636** ↔ 에뮬 명령어 수 비율 **1.6828** (1.2 % 차) |
| 하는 법 | 같은 spec·같은 시드로 실물과 에뮬을 수집하고, 에뮬의 `sample_map` 이 지목한 명령어 구간이 실측 파형의 어느 시점에 대응하는지 맞춰 본다 |
| 완료 판정 | 두 채널의 결과를 나란히 실은 보고서가 나오고, **일치·불일치 어느 쪽이든** 그 사실과 해석이 적힌다 |

### A10. EM 채널 (SEMA/DEMA) — 장비 구매 필요

| | |
|---|---|
| 현재 | 근접 자기장 프로브 미보유 → 공격 클래스 하나가 통째로 비어 있다 |
| 왜 | Annex E 는 오히려 **EM 을 선호**한다고 적는다 — 전력은 전역 신호이고 EM 은 국소 신호라 관심 회로만 볼 수 있기 때문이다 |
| 요건 | `[B.07]` — 프로브 대역폭이 IUT 클럭 이상 |
| 완료 판정 | `scope.channels` 에 `em` 이 들어가고 `scope.not_claimed` 에서 해당 줄이 빠진다 |

---

## B. 하드웨어 없이 확인·검증할 수 있는 것

### B1. `remask @ aes.c:282` 전이 누설의 원인 규명 ★ 최우선

이 프로젝트가 **실제로 찾아낸 것**이며, 결론을 내려면 사람이 소스를 읽어야 한다.

**관측된 사실**(002, 순열 20회로 재확인)

| 항목 | 값 |
|---|---|
| 위치 | `0x81be`, 성분 `hd_reg`(레지스터 전이), 명령어 #4671·#5267 |
| 소스 | `remask @ workspace/iut/masked-aes-c/aes.c:282` |
| 종속 민감값 | `add_rk0`(= p⊕k), `sbox_out`, `round1` |
| 통계량 | **∞** — 클래스 내 분산 0, 즉 완전 결정적 종속 |
| 구간 | CipherMasked (보호 대상 구간 안) |

**기계어**

```
81be:  ldrb  r4, [r0, #0]     ← state 바이트 로드
81c0:  eors  r4, r1
81c2:  strb  r4, [r0, #0]
81c4:  ldrb  r4, [r0, #1]     ← 같은 r4 재사용
```

`remask()` 의 루프가 **`r4` 를 매 바이트 재사용**한다. 두 share 가 같은 레지스터를
연달아 지나면 `HD = HW(share1 ^ share2) = HW(민감값)` 이 되는데, 그것이 이 프로젝트가
찾으려던 결함 유형 그대로다.

**아직 말할 수 없는 것** — 도구는 "여기를 보라" 까지만 했다.

- 정확히 **어느 두 값**이 XOR 되어 민감값이 되는지
- 이것이 실제 공격으로 이어지는지 — **공격자 관점 CPA 는 1/16 으로 실패했다.** 즉 이
  누설이 곧 키 복구를 뜻하지는 않는다
- 고칠 값어치가 있는지, 고친다면 어떻게(레지스터 분리, 배리어, 순서 변경)

**완료 판정**: 원인을 소스 수준에서 설명한 문서와, 수정 여부에 대한 판단이 남는다.
수정한다면 수정 전후 데이터셋으로 `soundness` 를 다시 돌려 그 지점이 사라지는지 확인한다.

**재현**

```bash
docker exec -it chipwhisperer-kor bash
cd "/workspace/[extra] Physical-AI-SCA"
arm-none-eabi-objdump -d --start-address=0x81b0 --stop-address=0x81e0 \
  emul_harness/build/masked-aes-c.elf
python3 -c "
from physai import spec as sm, soundness, paths
sp = sm.load('exp/002_emul_masked.yaml')
r = soundness.run(paths.TRACES/'002_emul_masked.h5', sp, n_traces=4000, n_perm=20,
                  sensitive_window=(0,10147), labels=('add_rk0','sbox_out','round1'))
print(r['candidates'])"
```

### B2. STM32F303 캐시 유무 확인

| | |
|---|---|
| 현재 | 대조표에서 **미기록**. 저장소 안에 근거 문서가 없어 확인하지 못했다 |
| 왜 | §8.2 는 대칭키 소프트웨어의 알려진 타이밍 위협을 cache-timing 으로 한정하고, 캐시가 없으면 그 프레임워크는 pass 라고 정한다. 그 면제의 **전제**가 이 사실이다 |
| 주의 | 이 항목과 무관하게 **§7.3.4 일반 타이밍 분석은 무조건 수행한다** — 둘은 다른 요구다 |
| 하는 법 | ST 레퍼런스 매뉴얼(RM0316 등)에서 데이터/명령어 캐시 유무를 확인하고 문서 번호·절을 근거로 남긴다 |
| 완료 판정 | 대조표의 `§8.2` 행이 `해당없음`(이유 포함) 또는 `미준수` 로 판정된다 |

### B3. DPA 트레이스 수 채우기 — 에뮬레이션은 지금도 가능하다

| | |
|---|---|
| 현재 | 세 실험 모두 DPA `inconclusive`. 보유 4,000장 vs Formula (1) 요구 **91,870장** |
| 왜 | 부족한 트레이스 수로 "누설 없음" 을 쓰는 것이 이 도구가 낼 수 있는 가장 나쁜 결과라 판정을 보류한다. **누설이 관측되었다면 그 사실은 트레이스 수와 무관하게 유효하다** |
| 비용 | 에뮬 tiny 약 31 ms/장 → TVLA 두 집단 각 46,000장이면 **약 48분**. masked 는 56 ms/장 → **약 86분**. 용량은 tiny 약 4.4 GB, masked 약 7.5 GB |
| 하는 법 | spec 의 `tvla_fk`·`tvla_rk` 를 각 45,935 이상으로 올리고 재수집 |
| 완료 판정 | 대조표의 `A.2.3 · Formula (1)` 이 준수로 바뀌고 DPA 가 `pass`/`fail` 을 낸다 |

### B4. SPA 육안 검사 — 사람이 해야 한다

| | |
|---|---|
| 현재 | 통계 검정만 수행하고 판정을 내지 않는다(`inconclusive`). `spa_traces.svg` 가 증거로 나와 있다 |
| 왜 | A.2.2 는 "육안 검사와 통계 검정 **둘 다** 통과" 를 요구하는데 육안은 정의상 사람의 행위다. **AI 가 "육안으로 확인했다" 고 쓰지 않는다** |
| 하는 법 | `runs/<id>/spa_traces.svg` 를 열어 같은 입력 쌍과 키가 다른 쌍의 파형을 비교한다 |
| 완료 판정 | 확인 결과와 판단 근거를 보고서에 사람이 적는다 |

### B5. Annex H 민감 누설 경계 확정

| | |
|---|---|
| 현재 | spec 이 1라운드를 경계 **안**에 두고 그 근거를 적었다. 대조표는 이것을 `범위밖`(제안일 뿐 확정 아님)으로 표시한다 |
| 왜 | Annex H.2(a) 는 블록암호의 **바깥 라운드**(예: AES 첫/마지막 1/3)를 비민감 예시로 드는데, 이 시험이 겨냥하는 1라운드 SBox 가 정확히 거기 들어간다. 이 경계가 **합/부를 직접 바꾼다** |
| 판단 근거(현재 spec) | (1) H 는 informative 이며 승인 기관이 대체 가능 (2) IUT 가 모듈이 아니라 라이브러리라 키 로딩 경계가 없음 (3) 1라운드 SBox 는 분할정복 키 복구의 표준 표적 |
| 완료 판정 | 사람 또는 승인 기관이 경계를 확정하고, 그 결정을 spec 의 `rationale` 에 반영한다 |

### B6. soundness 파라미터의 적정값 결정

| | |
|---|---|
| 현재 | 순열 6회·트레이스 2,000장으로 돌렸고, 확인용으로 20회·4,000장을 한 번 더 돌렸다 |
| 왜 | 귀무 임계를 순열 최댓값으로 세우므로 **순열이 적으면 임계가 덜 보수적**이다. 256 클래스에 2,000장이면 클래스당 7.8장이라 SNR 추정도 불안정하다 |
| 확인된 것 | 순열 6 → 20 으로 늘려도 `0x81be` 의 검출은 살아남았다(통계량 ∞). 즉 그 소견은 순열 수에 민감하지 않다 |
| 하는 법 | 순열 수와 트레이스 수를 바꿔 가며 검출 목록이 안정되는 지점을 찾고, 그 값을 기본으로 문서화 |
| 완료 판정 | 권장값과 그 근거가 `PROMPT.md` 에 적힌다 |

### B7. 검사 라벨 범위 확대

| | |
|---|---|
| 현재 | `add_rk0`·`sbox_out`·`roundkey0`·`roundkey1`·`round1` 다섯 가지 |
| 왜 | 2~10 라운드 state, 그리고 **마스크 값 자체**(연구자 관점에서 마스크가 예상 밖의 지점에 노출되는지)는 아직 보지 않았다. 결함이 그쪽에 있으면 지금은 놓친다 |
| 하는 법 | `soundness.run(labels=…)` 에 `round2`…`round10` 추가. 마스크 라벨은 `aes_ref.intermediates()` 밖이라 별도 처리 필요 |
| 완료 판정 | 확대한 라벨로 돌린 결과가 보고서에 실리고, 새 검출이 있으면 B1 과 같은 방식으로 추적한다 |

### B8. 명령어 수 고정 검증의 트레이스 확대

| | |
|---|---|
| 현재 | TA subset 각 1,000장(A.2.4 요건 충족)에서 **완전 고정** 확인 — tiny 6,030 / masked 10,147 |
| 왜 | 데이터 의존 제어흐름이 드물게만 나타나는 입력이 있을 수 있다. 트레이스 수가 늘면 신뢰가 오른다 |
| 참고 | 명령어 수가 달라지면 `sample_map` 이 무너져 누설 검정은 멈추지만 **TA 결과는 그대로 유효**하다(`EmulationTarget.run` 이 그 구분을 예외 메시지에 적는다) |
| 완료 판정 | Level 4 요건인 각 10,000장으로 돌려도 고정이 유지되는지 확인 |

### B9. LLM 실연동 확인 — grok 헤드리스 / Gemma 4 31B

| | |
|---|---|
| 현재 | `physai/llm.py` 가 있고 보고서에 연결되어 있으나, **실제 엔드포인트로 호출해 본 적이 없다.** 환경변수가 없으면 그 사실을 적고 넘어간다 |
| 왜 | 온라인↔오프라인 전환 비용이 환경변수 세 개라는 설계 주장을 **실측으로 확인해야 한다** |
| 하는 법 | `PHYSAI_LLM_BASE_URL`·`PHYSAI_LLM_MODEL`·`PHYSAI_LLM_API_KEY` 를 주고 `report` 를 다시 돌린다. 오프라인은 Ollama(`http://localhost:11434/v1`) 또는 vLLM |
| 주의 | LLM 이 쓴 문단은 **반드시 그 사실이 표시**된다. 수치·판정·해시·대조표는 도구가 만든 것이며 LLM 이 그것을 바꾸지 않는지 사람이 대조한다 |
| 완료 판정 | 두 환경 모두에서 서술 절이 생성되고, **코드 변경 없이** 전환되는 것을 확인 |

### B10. grok 헤드리스 CLI 와의 실제 결합

| | |
|---|---|
| 현재 | 확인하지 못했다(이 작업 중 웹 접근을 하지 않았다). 그래서 특정 CLI 에 결합하지 않고 "하네스 교체 가능" 으로 설계했다 |
| 왜 | 이 환경의 전제는 "AI 가 CLI 와 계약 파일을 손잡이로 쓴다" 이고, 그것이 실제 하네스에서 도는지는 돌려 봐야 안다 |
| 하는 법 | grok 헤드리스(또는 다른 에이전트 CLI)에 `AGENTS.md`·`PROMPT.md` 를 주고 한 사이클(①설계 → ⑥검증)을 사람 개입 없이 돌려 본다 |
| 완료 판정 | AI 가 스스로 `exp/*.yaml` 을 쓰고 네 CLI 를 호출해 보고서 3종을 만든다. 막힌 지점은 `PROMPT.md` §8 에 추가한다 |

---

## C. 계획과 달라진 점 (기록)

구현하며 사실을 확인해 계획을 바꾼 것들이다. 판단 근거를 남긴다.

| 계획 | 실제 | 이유 |
|---|---|---|
| 빌드 플래그 `-O2` | **`-Os`** | 실측 펌웨어가 `-Os` 다(`workspace/base/Makefile.inc`). 최적화 수준이 전이 누설을 만들고 없애므로, 플래그가 다르면 다른 구현을 분석하는 셈이다 |
| `templates/{01,02,03}.md` | **만들지 않음** | `report.py` 가 문서를 코드로 조립한다. 대조표·수치·조건부 절이 대부분이라 템플릿과 코드로 나누면 두 곳을 고쳐야 한다. 쓰이지 않는 `TEMPLATES` 상수는 삭제했다 |
| `contracts/results.schema.json` | **만들지 않음** | 소비자가 없다. `results.json` 의 계약은 `grades` 필드와 `json_conventions` 가 문서화하고 있으며, 검증하는 코드가 생기면 그때 만든다(AGENTS.md 원칙 2) |
| SPA 가 `pass`/`fail` 판정 | **판정하지 않음** | 잡음 바닥이 0 인 결정적 채널에서 "키가 다르면 파형도 다르다" 는 거의 항상 참이라, 그것만으로 fail 을 내면 **어떤 구현도 통과할 수 없는** 판별력 없는 시험이 된다. 관측값을 `statistical_verdict` 로 보고하고 판정은 사람이 한다 |
| 앞 시험 fail 시 뒤 시험 중단 | **계속 수행** | §7.3.2 의 순서는 `should` 이고 §8.1 의 "셋 다 평가" 는 `shall` 이다. 건너뛰면 §8.1 을 어기고 "어디가 얼마나 새는지" 정보를 잃는다 |
| A.2.5 를 `미준수` 로 예상 | **`미기록`** 으로 나옴 | 1.0 데이터셋에는 `preprocessing_average_n` 필드 자체가 없다. "안 지켰다" 가 아니라 "모른다" 가 정확하다 |

---

## D. 이 환경을 쓸 때 잊지 말 것

- **`inconclusive` 는 "안전" 이 아니다.** 트레이스 수 부족이나 육안 미결은 판정할 수 없다는 뜻이다.
- **"검출 없음" 은 대조군 증거와 함께만 의미가 있다.** `exp/001`(비마스킹)이 그 증거다.
- **경계 밖 검출을 감추지 않는다.** Annex H 에 따라 fail 로 세지 않되 별도 목록으로 보고한다.
- **에뮬에서 깨끗해도 실물에서 샐 수 있다.** 세 관측은 대체재가 아니라 상보재다.
- **"검증 완료" 라고 쓰지 않는다.** 이 환경이 낼 수 있는 것은 사전 진단 결과다.
