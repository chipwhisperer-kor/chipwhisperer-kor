# 데이터셋은 GB 단위라 커밋하지 않는다. collect 로 재생성한다.
traces/*.h5
# 실행 산출물 — 보고서·그림·중간 배열. 필요하면 report 로 다시 만든다.
runs/
# 에뮬레이션 ELF 는 빌드 산출물이다. make -C emul_harness IUT=<name> 로 만든다.
emul_harness/build/
__pycache__/
*.pyc
