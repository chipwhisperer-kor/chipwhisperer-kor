# 여기 쌓이는 .npz 는 전부 노트북이 만드는 중간 산출물이라 추적하지 않는다.
# `poi_*.npz` 는 `1.0.SNR` 이 타겟별로 쓰고 `2.0`~`6.0` 이 읽는데, Dataset(h5)만
# 있으면 `1.0` 을 다시 돌려 그대로 얻는다. Dataset 자체가 Git 에서 제외되므로
# 그 파생물만 커밋해 두면 무엇으로 만든 것인지 확인할 방법이 없다.
*.npz
