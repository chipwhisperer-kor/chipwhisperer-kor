# DECISIONS — 사용자 결정·피드백 로그

형식: `YYYY-MM-DD | 결정 | 출처`

---

## 2026-07-21 — 프롬프트 설계 단계 확정

| # | 결정 | 출처 |
|---|------|------|
| D1 | 대상 논문 시작: **양자 내성 암호 HAETAE에 대한 오류 주입 공격 및 대응 기법** (`HAETAE-FIA`) | 사용자 |
| D2 | 레퍼런스는 본 논문 명시 항목만 **1-deep** | 사용자 |
| D3 | 레퍼런스 PDF 부재 시 **반드시 사용자에게 요청**. AI의 웹 검색·다운로드 금지. 확보 불가 시 사용자가 설명 | 사용자 |
| D4 | 심층 읽기·슬라이드 반영 게이트: **한 문단마다** 승인 | 사용자 |
| D5 | 전문 용어, 기호, 알고리즘명, 고유명사, 사람 이름: **영문 원본 표기 유지** | 사용자 |
| D6 | 발표자료: 한글 Marp, 템플릿 `Presentation_Marp/0. Template` (구: `Marp with LaTeX.css/0. Template` → **D24** 갱신) | 설계 문서 → D24 |
| D7 | 본문 비요약·비축약·비상상; 레퍼런스만 요약 허용 | 설계 문서 |
| D8 | 중간 산출물: `.Intermediate_Artifacts/` 지속 유지·갱신; 마일스톤·로드맵 필수 | 설계 문서 |
| D9 | 후순위 논문: Public Coefficient Matters … 은 HAETAE-FIA 완료 후 | 작업 순서 합의(시작 논문 지정) |

---

## 2026-07-21 — H-M1 승인

| # | 결정 | 출처 |
|---|------|------|
| D10 | OUTLINE confirmed. V=대응 방안, VI=결론. 알고리즘·정리·증명 독립 P-ID | 사용자 `confirmed` |

## 2026-07-21 — 경로·소통 기본설정

| # | 결정 | 출처 |
|---|------|------|
| D11 | ~~산출물 **정식 경로 = 메인 저장소** `/home/user/fia_cm_haetae/Collabo_HB`. worktree 작업 후 즉시 동기화.~~ **→ D41로 폐기.** 루트 기준 상대경로, 절대경로는 `SYNC.md`에 격리 | 사용자 |
| D12 | 사용자↔AI 소통·검토는 **항상 Markdown**. CLI로 내용 검토 유도 금지 (**유효** → `PROMPT` §3.7). ~~`COMMS/LATEST.md` 매 턴 갱신~~ **→ D41로 폐기** | 사용자 |

## 2026-07-21 — 사용자 단일 접점

| # | 결정 | 출처 |
|---|------|------|
| D13 | 사용자 접점 = ~~**`To_Do.md`만**~~ **→ D40로 역할별 3파일 분리.** 판단 항목 기본 1·최대 2 (**유효**, 파일당). Intermediate·COMMS는 AI 내부 | 사용자 |

## 2026-07-21 — H-M2 승인

| # | 결정 | 출처 |
|---|------|------|
| D14 | H-M2 confirmed. 73단위·권장 기본값(각주·참고문헌·저자소개 유지, 무거운 단위는 슬라이드 분할). | 사용자 `계속` |

## 2026-07-21 — `계속` ↔ 답변 칸

| # | 결정 | 출처 |
|---|------|------|
| D15 | 채팅 `계속` = `To_Do.md`의 `## 사용자 답변 칸` 이하를 읽어 진행. CLI 최소. `#### 답하는 방법`에 명시. | 사용자 |
| D16 | 답변 칸 `작업 계속` → P-001 승인·다음 문단 진행 | 답변 칸 내용 |

## 2026-07-21 — 발표 제외 지시

| # | 결정 | 출처 |
|---|------|------|
| D17 | 영문 ABSTRACT(P-003)는 **발표자료에서 제외**. 해석·READING만 유지. | 답변 칸 |
| D18 | Keywords(P-004)도 **발표자료에서 제외**. | 답변 칸 `발표자료 제외` |
| D19 | 연구비·접수 각주(P-005)도 **발표자료에서 제외**. | 답변 칸 `발표자료 제외` |
| D20 | 레퍼런스 PDF: `Papers/<대상논문제목>/`, 파일명 `[<n>] <제목>.pdf`. 웹 수집 금지. 요약 슬라이드 전 검수. | 답변 칸 (P-009 턴) |

## 2026-07-21 — 본문 보류 · [9–12] 선행

| # | 결정 | 출처 |
|---|------|------|
| D21 | P-010 보류. [9]–[12] 요약 선행. 요약 전 **핵심 포인트(A 대상연산, B 키복구 로직 + 추가항목)** 합의. Dilithium FI 슬라이드 보강. | 답변 칸 |
| D22 | 요약 패키지: **A+B+C+D+E+G**, F는 확실할 때만. 슬라이드 수 **상한 없음** (배경 이해 우선). | 답변 칸 |
| D23 | **알고리즘 단계별 발표 풀이 제외.** 참조(§·Fig·[2])만 명시하고 진행. 키생성(P-012)부터 적용. | 답변 칸 |

## 2026-07-22 — 템플릿 이전·발표자료 전면 재작성

| # | 결정 | 출처 |
|---|------|------|
| D24 | 템플릿 경로: `Marp with LaTeX.css/` → **`Presentation_Marp/`**. 예제 템플릿 전면 개편(Markdown+최소 CSS, LaTeX.css/외부 폰트 제거). 기존 HAETAE 발표자료 **전면 재작성**. 수식은 **`$...$` / `$$...$$`만** (`\(...\)` 금지). 피드백 반영 완료 후 P-014 재개. | 답변 칸 (2026-07-22) |

## 2026-07-22 — Dilithium FI 심층 선행 (표준 기반)

| # | 결정 | 출처 |
|---|------|------|
| D25 | HAETAE 코어(P-014+) 전에 **FIPS 204 알고리즘 의사코드**를 기준으로 레퍼런스 **[9]–[12]** 오류주입 로직을 심층 분석. 순서: **발표 연도 빠른 순** `[11]→[9]→[10]→[12]`. 각 공격: (1) FI 대상 라인 (2) C코드 있으면 (3) 값 영향 (4) 전파 (5) 키복구. 알고리즘은 표준과 **동일 표기**. 논문은 정답지 아님·비판적 검증. **D23 예외 트랙:** Dilithium deep-dive에서만 알고리즘 라인 풀이 허용. P-014·HAETAE 본문 보류. | 답변 칸 (2026-07-22 `계속`) |

## 2026-07-22 — 발표 용어·표준 완결성·구현 한계

| # | 결정 | 출처 |
|---|------|------|
| D26 | **용어:** 일상 서술은 한글. 전공·함축 뉘앙스가 있는 전문 용어는 **영문 원형 + italic** (`*NTT*`, `*twiddle*`, `*pointwise multiplication*` 등). 애매한 한글 조어(예: “점별곱”) 지양. 이후 전 슬라이드·To_Do에 적용. | 답변 칸 |
| D27 | Deep-dive에서 인용하는 **FIPS 알고리즘은 수정 없이** 발표자료에 수록 (동료가 표준 PDF를 펼치지 않아도 완결). 길면 슬라이드 분할만 허용, 줄 생략·의역 금지. | 답변 칸 |
| D28 | *Fault injection* 성공의 **구현 의존성**은 인정하고 논의 진행. 다만 그 **한계는 슬라이드에 명확히** 적는다 (대수 모델 vs 구현 성공을 혼동 금지). | 답변 칸 |
| D29 | DIL-11-U1 확정 (Alg 41 전문 슬라이드 반영 완료). 다음: U2 Attack-1. | 답변 칸 지시 이행 |
| D30 | 발표 **목차 재구성:** 맨 앞 **「들어가기에 앞서 — Dilithium FI Deep-Dive [9]–[12]」** → 이후 대상 논문(요약·I·II…). U2 등 deep 단위는 구조 승인 후 재개. | 답변 칸 (구조 교정) |
| D31 | **논리 청크 규칙:** 매 논리 단위의 시작은 현실 자료(논문·표준 알고리즘·구현/실험)를 제시한 뒤 해석. (표기는 청중용 **「출처:」**, “존재 근거” 금지 — D32) | 답변 칸 |
| D32 | **발표 자기완결성 (Self-containment):** 보이는 슬라이드는 청중 배포용 완결 문서. 메타 발언·제작 프로세스·승인/예정·히스토리·폴더 경로·AI 지침 금지. 내부 정보는 HTML 주석 또는 To_Do/Intermediate만. [9]–[12]는 **소표지 1개**(들어가기에 앞서) 아래 일체감 있게 서술. | 답변 칸 |
| D33 | 레퍼런스 정리 항목: **주입 대상 · 키 복구 · 서명 모드 · 오류 횟수 · 공격 실증 · HAETAE 연계 · 한계** (한글 1–2어). *서명 모드*=det/rand/hedged. *오류 횟수*=fault·오류 서명 수. *공격 실증*=SW/HW 실증. A–G 폐지. 범례 필수. | 답변 칸 |

## 2026-07-23 — Papers 아카이브 전량 확보

| # | 결정 | 출처 |
|---|------|------|
| D34 | 사용자 `Papers/` 보강 완료를 작업 상태에 반영. **HAETAE-FIA** 1-deep PDF **24/24**, **PCM-DFA** 대상 PDF+1-deep **35/35**([8][35] PDF 비대상), 공통 표준문서 FIPS 204 1편. 출처·크기는 `Papers/reference-download-manifest.csv`·폴더 README. 매니페스트 디스크 대조 OK. **이후 1-deep PDF 요청 불필요.** AI 웹 수집 금지(D3/D20)는 유지. PCM 본문 작업은 여전히 HAETAE-FIA 완료 후(D9). | 답변 칸 |


## 2026-07-23 — 멀티 AI 연구환경 (3역할)

| # | 결정 | 출처 |
|---|------|------|
| D35 | **3-AI 연구환경.** role_id=`curator`(학술정보·연구자산 큐레이터), `analyst`(연구문헌 심층분석가), `producer`(Marp 프레젠테이션 프로듀서). 현재 서비스 바인딩: Codex / Grok / Agy. 흐름: 수집·검증·정리 → 분석·해석 → 구조화·시각화·발표자료. 각 역할은 자기 책임만; 타 산출물은 입력 전용·무단 개서 금지. **서비스명·역할 개수는 가변** — 기준 문서는 `AI_ROSTER.md` + `roles/`. 인계는 `HANDOFF.md`. 사용자 접점은 계속 `To_Do.md` only. 레거시 단일AI 산출물(P-001–013, Dilithium slides 등)은 유지하되 **이후 신규 작업은 역할 분리**. | `To_Do.md` 사용자 답변 칸 |

## 2026-07-23 — 멀티 AI 아키텍처 v2 검수

| # | 결정 | 출처 |
|---|---|---|
| D36 | D35 역할 정의를 운영 가능한 계약으로 보강한다. 공통 시작 프롬프트, 지침 우선순위, 역할별 단독 쓰기 영역, 공통 handoff 상태, 자산·분석·발표 패킷의 완료 기준, 근거 추적 체인을 적용한다. curator는 승인된 연구 범위 안에서 공개적·적법한 외부 자료를 개별 검색·수집할 수 있으나 출처·버전·접근·라이선스·SHA-256을 기록해야 한다. D3/D20의 사용자 원본 우선, 기존 원본 무단 교체 금지, 무단 대량 수집 금지는 유지한다. analyst와 producer는 외부 자료를 직접 수집하지 않는다. | 사용자 현재 요청의 역할 정의 및 `Paper-Deep-Dive/` 검수·교정 지시 |

## 2026-07-23 — 아키텍처 v3 (SSOT·압축·충돌 해소)

| # | 결정 | 출처 |
|---|------|------|
| D37 | **지침 v3.** (1) 문서 SSOT 계층: README 지도 / BOOTSTRAP 역할원문 / PROMPT 계약 / AI_ROSTER 소유 / ARTIFACT 패킷 / roles 실행. 동일 장문 4중 복붙 금지. (2) worktree·Canonical 분열 시 **Canonical 우선**, 이후 worktree 동기. (3) Intermediate 중 `papers/`만 analyst 단독; HANDOFF·MILESTONES 등은 공유 행 규칙. (4) 수집 정책은 D36 유지(D3/D20의 전면 웹금지는 수집 범위에서 대체; 원본 우선·무단 교체·대량 수집 금지는 유지). (5) 연구 게이트 G1–G10을 PROMPT에 압축 수록. (6) `Papers/ASSET_CATALOG.md` 도입. (7) ROSTER_STATE는 포인터만. (8) `.Prompt_Engineering`은 역사 자료, 운영 구조로 따르지 않음. (9) 서비스명 변경·역할 수 변경은 AI_ROSTER. | 사용자: 3-AI 동일 프롬프트·검수·압축·충돌 확인 요청 |

## 2026-07-23 — 아키텍처 v3.1 운영 보강

| # | 결정 | 출처 |
|---|------|------|
| D38 | 세 AI가 동일 요청을 받아도 쓰기 소유권은 유지한다. 공통 정책 문서는 curator가 통합하고 analyst·producer는 자기 역할 문서 수정 또는 handoff 제안만 한다. 현재 대화의 명시적 사용자 요청을 지침 우선순위에 포함한다. `ARTIFACT_CONTRACTS`는 handoff 생성·소비 시에만 읽는다. 신규 파이프라인의 첫 연구 작업은 curator가 DIL-10 필수 자산을 재검증하여 카탈로그와 `ready` 패킷을 만드는 것이다. analyst는 자산 `ready`와 사용자 게이트가 모두 충족된 뒤 착수한다. | 사용자: 동일 프롬프트로 다중 AI 검수, 충돌 제거, 컨텍스트 압축 요청 |

## 2026-07-23 — 3-AI 부트스트랩 완료

| # | 결정 | 출처 |
|---|------|------|
| D39 | Codex=`curator`, Grok=`analyst`, Agy=`producer` 부트스트랩 완료. 신규 역할 파이프라인을 활성화하고 curator의 DIL-10 자산 패킷부터 진행한다. | 사용자 `부트스트랩 완료` |

## 2026-07-24 — 사용자 접점 역할별 분리

| # | 결정 | 출처 |
|---|------|------|
| D40 | **접점 파일을 역할별로 분리한다.** `To_Do.md` 단일 접점을 폐지하고 `To_Do_Curator.md` · `To_Do_Analyst.md` · `To_Do_Producer.md`로 나눈다. 사유: 세 AI의 작업 완료 시점이 어긋날 때 한 역할에게 쓴 지시를 다른 역할이 덮어써 오염되는 사고가 있었다. **각 접점 파일은 해당 역할의 단독 쓰기 영역**이며(공유 아님) 사용자만 `## 사용자 답변 칸`에 쓴다. 각 역할은 **자기 파일만** 읽고 쓰고, 타 역할 접점 파일은 열지 않는다 — 역할 간 조율은 계속 `HANDOFF.md`. 단일 파일 전제였던 「미결 전체 1건·비어 있을 때만 갱신·타 역할 질문 덮어쓰기 금지」 규칙은 **삭제**하고, 미결 질문은 **파일당 1건(최대 2)**으로 둔다. 채팅 `계속` = **자기** 접점 파일의 답변 칸. 답변 칸은 반영 후 비우고 결과를 `최근 완료`에 한 줄 남긴다. D13/D15의 "접점은 하나" 취지는 **사용자가 볼 파일은 접점 파일뿐**으로 유지된다(개수만 3). 고정 섹션: `지금 상태`·`지금 할 일 (사용자)`·`다음에 올 일`·`최근 완료`·`사용자 답변 칸`. | 사용자 직접 대화 (2026-07-24) |

## 2026-07-24 — 자기완결성(Self-contained) 확정 · 가동 전 최종 검수

| # | 결정 | 출처 |
|---|------|------|
| D41 | **자기완결성을 프로젝트 필수 속성으로 확정한다.** 판정 기준: *이 폴더만 넘겨받은 제3의 AI가 외부 정보 없이 곧바로 이어서 작업할 수 있는가.* 규칙 S1–S6은 `PROMPT.md` §0. 파생 조치: (1) **D11 폐기** — 절대경로 Canonical(`/home/user/fia_cm_haetae/Collabo_HB`) 지정을 폐기하고, 프로젝트 루트 = `README.md`가 있는 폴더, 모든 경로는 **루트 기준 상대경로**. 환경 의존 절대경로는 `.Intermediate_Artifacts/SYNC.md` 한 곳에만 격리하며 운영 지침이 아니다. `README_PATH.md`는 전량 중복+절대경로뿐이라 **삭제**. (2) **D12 부분 대체** — 「Markdown 소통·CLI 검토 유도 금지」는 `PROMPT` §3.7로 승격해 유지, 「`COMMS/LATEST.md` 매 턴 갱신」 의무는 **폐지**(단일-AI 시절 산물; 진행 상황은 자기 `To_Do_<Role>.md`·`HANDOFF`·`PROGRESS`가 대체). `COMMS/`는 역사 계층. (3) `BOOTSTRAP_PROMPT.md`의 역할 정의를 서비스명(Codex/Grok/Agy) 고정에서 **`role_id` 기준**으로 전환 — 표에 없는 신규 AI도 `AI_ROSTER.md` 바인딩 추가 후 합류 가능. (4) 역할 미확정 AI는 **어떤 파일도 쓰지 않고 대화로 확인 후 대기** — 접점 파일이 전부 역할 소유가 된 D40 이후의 교착 제거. (5) `README.md` 문서 계층을 **운영/역사/산출물** 3분류로 확장해 고아 파일 제거(`AGENTS`·`CLAUDE`·`CROSS_CHECK`·`ASSET_CATALOG*`·`0. Template`·`COMMS`·`SYNC`·`ROSTER_STATE`·`ARCHITECTURE_AUDIT`·`.Prompt_Engineering`·`style_enhancer.py`). (6) `CROSS_CHECK`를 `PROMPT` §8.2 공유 규칙 표에 명시(기존 `AI_ROSTER`의 「PROMPT §8」 참조가 가리킬 행이 없었음). (7) `style_enhancer.py`를 **producer 단독 쓰기**로 귀속하고 하드코딩 경로·제자리 덮어쓰기·선택 도구임을 `roles/producer/PROMPT.md`에 명시. | 사용자 직접 대화 (2026-07-24) — 가동 전 최종 검수 지시 |

## 2026-07-24 — `director` 역할 신설

| # | 결정 | 출처 |
|---|------|------|
| D42 | **4번째 역할 `director`(연구 프로젝트 총괄 디렉터)를 신설하고 Claude에 바인딩한다.** 업무: (a) 세 역할이 준수할 산출물 규격·명명/저장 규칙·인용/출처 표기 기준·품질 판정 기준·역할 간 인계 조건을 정의하고 문서화, (b) 각 단계 종료 시 규격·책임 범위 충족 여부를 검증해 진행 **승인** 또는 **사유+보완 방향 명시 반려**, (c) 역할 간 책임 중첩·판단 충돌의 조정과 **최종 결정**. **금지:** 자료 수집·논문 분석·발표자료 작성 등 산출물 **내용의 직접 생성·수정**. 반려 시에도 방향만 제시하며 수정은 담당 역할이 수행한다. **파이프라인 외부·상시 아님** — 마일스톤 종료, 규격 개정, 상위 계층(구조·룰·지침) 변경, 역할 충돌, 산출물 누적, 사용자 호출 때만 관여하고 그 외에는 세 역할이 정상 진행한다. 구조 반영: (1) 접점 파일 `To_Do_Director.md` 추가(역할당 1파일 원칙 유지, D40). (2) **공통 정책 문서(`README`·`PROMPT`·`AI_ROSTER`·`ARTIFACT_CONTRACTS`·`BOOTSTRAP_PROMPT`·`AGENTS`·`DECISIONS`)와 `CROSS_CHECK`의 소유를 curator → director로 이관** — 규격 문서화가 director의 정의된 업무이므로 통합자와 소유자를 일치시킨다. 세 역할은 `to: director` handoff로 개정을 제안한다. (3) **새 handoff 상태를 만들지 않는다** — 기존 `gate` 열에 `director`를 적고 `awaiting_approval` → `ready`(승인) \| `blocked`(반려)로 전이. (4) **이중 게이트 금지** — 분석 단위별 승인(G1)은 종전대로 사용자 게이트이며 director를 거치지 않는다. director 게이트는 **마일스톤 단위**. (5) `MILESTONES`의 M-항목 `done` 전환은 director 승인 handoff를 근거로 한다. (6) 접점 파일 읽기 예외: director는 충돌 조정에 필요할 때 타 역할 접점 파일을 **읽을 수 있으나 쓰지 않는다**(읽기는 오염을 만들지 않음). | 사용자 직접 대화 (2026-07-24) |
| D43 | 신규 논문의 **`paper_id` 발급 주체는 `curator`**. analyst는 발급된 ID로 `.Intermediate_Artifacts/papers/<paper_id>/META.md`를 작성한다. 근거: `paper_id`는 `ASSET_CATALOG`·`Papers/assets/<paper_id>/` 등 curator 산출물의 키로 먼저 쓰인다. `.Prompt_Engineering/`은 사용자가 삭제 — 문서 계층의 역사 항목에서 제거. | 사용자 직접 대화 (2026-07-24) |

## 2026-07-24 — 재가동 전 구조 검수 (director 판정)

| # | 결정 | 출처 |
|---|------|------|
| D44 | **재가동 전 룰·구도 검수 결과 반영.** D40–D43 도입 과정에서 생긴 불일치를 시정한다. (1) **`HANDOFF`의 `gate` 열 정의 확정** — 값은 `—`\|`user`\|`director` **한 값**이며 서술문 금지, 검증 내역·요청 사유는 `payload`로. D42가 `gate`에 형식 의미를 부여했으나 기존 2행은 자유 서술이라 스키마가 모순이었음. 기존 2행은 **레거시 형식**으로 명시하고 새 행부터 적용. (2) `AI_ROSTER`의 `paper_id` 발급 규칙을 **공유 파일 표 → curator 단독 쓰기 비고**로 이동(파일 공유 규칙이 아니라 소유 규칙), 오기 `D42`→**`D43`** 정정. (3) `BOOTSTRAP` 독해 순서 4의 "타 역할 접점 파일은 보지 않음"을 **"쓰지 않음"**으로 교정 — `PROMPT` §8.1의 director 읽기 예외와 정면 충돌했고, 부트스트랩만 읽은 director가 잘못된 규칙을 따르게 됨. (4) `MILESTONES`에 **M-항목 `done` 전환 = director 승인 근거** 규칙 명시, H-M7 책임을 "역할별 근거 → director 판정"으로, 포인터에 director 행 추가. **마일스톤/handoff/분석단위 세 상태 어휘가 별개임을 명시**(`approved` vs `done` 혼동 방지). (5) `ROADMAP` 흐름도에 director 단계와 `H-` 접두어 체계 명시. (6) `CROSS_CHECK` #10은 director 소유 문서를 대상으로 하므로 **근거 제출을 curator가 유지**해 자기검증을 회피. (7) `PROMPT` §6에 **사용자 > director > 계약 문서** 서열과 "기록되지 않은 director 판단은 무효(S3)" 명시. (8) `roles/analyst/PROMPT.md`의 상태 혼입은 **analyst 단독 쓰기 영역이므로 director가 고치지 않고** `HO-20260724-01`로 회송. | director 판정 (사용자 `To_Do_Director.md` 답변 칸 검수 지시) |

## 2026-07-24 — 1사이클 가동 검증 (director 판정)

| # | 결정 | 출처 |
|---|------|------|
| D45 | **D40–D44 반영 후 첫 1사이클 판정.** 변경 6파일 전수 대조 결과 **교차 오염 0건** — 어떤 역할도 타 역할 소유 파일을 쓰지 않았고, curator는 director로 이관된 `AGENTS.md`를 인지하고 수정하지 않았다. D40의 목적(접점 오염 제거)과 D42의 소유 이관이 실제로 작동함을 확인. 역할별: **curator 승인**(`HO-20260724-02` 정확 이행 → `done`, 기록·비움 완료), **producer 반려**(`To_Do_Producer.md`의 답변 칸 **코드 펜스 삭제** → 사용자 입력 통로 소멸; `HO-20260724-03`으로 회송), **analyst 미수행**(답변 칸 미소비·미비움, `HO-20260724-01` `ready` 방치, 기록 없음). 시정: (1) **규격 결함을 director가 자인하고 수정** — §8.1의 "답변 칸을 비운다"가 펜스 삭제로 오해될 수 있었다. "**내용만** 비운다 · 고정 섹션 삭제·개명 금지 · 펜스는 구조물"로 명확화하고 비운 상태의 3줄 형태를 명시. producer의 위반은 이 모호함이 유도한 것이므로 규격 수정을 선행한다. (2) 답변 칸 비운 상태 표기를 `(여기에 기입)`으로 통일(curator는 공백, producer는 삭제로 제각각이었음). (3) handoff 수신 전이는 `ready` → `in_progress` → `done`이나 curator가 `in_progress`를 건너뜀 — 1자 수정 규모에서는 문제 없으므로 규칙은 유지하되 강제하지 않는다. | director 판정 (사용자 `To_Do_Director.md` 답변 칸 사이클 검증 지시) |

## 2026-07-24 — bare `계속` 전환 대비 재검수 (director 판정)

| # | 결정 | 출처 |
|---|------|------|
| D46 | **사용자가 파일명 없이 `계속`만 보내는 운용으로 전환함에 따라 그 경로를 재검수하고 3건을 시정한다.** (1) **§3.5 ↔ §7.2 충돌 해소** — §3.5는 "답변 칸이 비면 대기", §7.2는 "자기 대상 `ready`도 처리"로 서로 달랐다. 확정: 답변 칸이 비었다는 것은 **새 사용자 지시가 없다는 뜻일 뿐**이며, 자기 대상 `ready` handoff는 그대로 처리하고 **둘 다 없을 때만 대기**한다. 이로써 `계속` 한 마디로 밀린 회송이 자동 처리된다. (2) **§8.1 "반영한 뒤 비운다" → "처리한 뒤 비운다"** — "반영"이 산출물 변경을 함의해, "파악하라·검토하라"처럼 변경이 없는 지시에서는 비우지 않아도 된다고 읽혔다(D45의 analyst 미수행이 이 해석과 일치). 산출물 변경이 없는 지시도 처리에 포함되며 비우고 `최근 완료`에 남긴다. (3) **`계속` 정의를 첫 접촉 문서로 확산** — 그동안 `PROMPT` §3.5와 접점 파일 헤더에만 있어, `AGENTS.md`(Codex 자동 로드)·`BOOTSTRAP_PROMPT.md`(신규 AI 최초 전달)만 읽은 AI는 bare `계속`을 해석할 근거가 없었다. 두 문서에 정의·빈 칸 동작·펜스 보존을 한 줄씩 추가. | director 판정 (사용자 `To_Do_Director.md` 답변 칸 재검수 지시) |

## 2026-07-24 — 두 명제 명문화 · director 자기 위반 시정

| # | 결정 | 출처 |
|---|------|------|
| D47 | **「자기완결성」과 「Simplicity is the ultimate sophistication」을 프로젝트 전체를 관통하는 두 명제로 확정하고 `PROMPT` §0에 나란히 명문화한다.** 계기: director가 D45–D46 검수 결과·제안 명령을 **채팅에만** 남기고 `To_Do_Director.md`는 "지금 할 일 0건"으로 두었다. 접점 파일만 보는 사용자에게는 아무 일도 없던 것으로 보이며, 이는 director 자신이 만든 S3 위반이다. 조치: (1) **S3을 양방향으로 개정** — 기존에는 사용자→AI 방향만 규정했다. AI→사용자 방향을 추가: **보고·판정·제안의 SSOT는 자기 `To_Do_<Role>.md`이며, 채팅 답변은 그 파일 내용의 전달일 뿐이고 채팅에만 있는 보고는 수행하지 않은 것으로 간주한다.** 사용자는 편의상 채팅으로 명령할 수 있으나 AI의 보고는 접점 파일만으로 완결되어야 한다. (2) **§0.2 단순성(P1–P5) 신설** — P1 한 사실은 한 곳에 · P2 규칙보다 구조(D40 선례) · P3 기존 것을 먼저 재사용(D42 `gate` 열 선례) · P4 장식 금지 · P5 표와 포인터. 그동안 암묵적으로 적용해 온 판단 기준을 명시해 모든 역할이 같은 잣대를 쓰게 한다. (3) **§8.1 보강** — `지금 할 일 (사용자)`는 실제 할 행동·판단이 있으면 반드시 채우며 "채팅으로 말했다"는 이유로 `0건`으로 두지 않는다. `최근 완료`는 이 파일만 읽고 경위를 알 수 있을 만큼 적고 상세는 D-번호로 가리킨다. (4) `README`·`CLAUDE`에 두 명제 반영. (5) `To_Do_Director.md`를 규격대로 재작성 — 할 일 2건(producer `계속` / DIL-12 선행 여부) 게시. sparse-c-NTT는 이미 `To_Do_Analyst.md`에 있으므로 **중복 게시하지 않는다**(P1). | 사용자 지적 (2026-07-24) — 접점 파일만으로 자기완결적 보고가 되어야 한다 |

## 2026-07-24 — 2사이클 검증 (director 판정)

| # | 결정 | 출처 |
|---|------|------|
| D48 | **설계 실증 성공 + 규격 결함 2건 자인·시정.** **핵심:** 답변 칸이 파괴돼 사용자가 지시를 쓸 수 없던 producer가, **빈 답변 칸 + bare `계속`만으로** 자기 대상 `ready`(`HO-20260724-03`)를 집어 **자기 접점 파일을 스스로 복구**했다. D46(빈 칸이어도 `ready`는 처리)이 의도대로 작동했고, director가 남의 파일을 건드리지 않고 파손이 해소되는 경로가 실증됐다. 역할별: **producer 승인**(펜스 복구·중복 2줄 병합·`HO-...-03` → `done`), **curator 조건부 승인**(`ASSET_CATALOG`의 `HAETAE-FIA-REF-12`가 스키마 20개 필드 완비, `license: unknown`을 추측으로 채우지 않고 `access: institutional`을 정직하게 기재, 경로는 루트 상대경로 — 규격 충족. 다만 `HO-20260724-04` 행이 **9칸**이라 `HO-20260724-05`로 회송), **analyst 2사이클 연속 미수행**(`ready` 3건 방치, 답변 칸 미소비). 시정 — 두 결함 모두 **director 규격 탓**으로 판정한다: (1) `(여기에 기입)` 안내 문구는 기능이 없는 **장식**(P4)이었고 3역할 중 2역할이 독립적으로 빈 펜스를 택했다 → 요구를 **선택 사항으로 완화**하고, 지켜야 할 것은 **펜스 자체**임을 분명히 한다. (2) D44에서 레거시 2행을 "표시만" 하고 남긴 결정이 실제로 오류를 유발했다 — curator가 인접 레거시 행의 형태를 모방하면서 새 `gate` 규칙도 지키려다 칸이 하나 늘었다. 규칙을 더하는 대신 **표 첫 줄에 템플릿 행을 넣어 본보기 자체를 교체**한다(P2 규칙보다 구조). "행은 정확히 8칸" 명시. | director 판정 (사용자 `To_Do_Director.md` 답변 칸 검수 지시) |

## 2026-07-24 — 동기화 규칙 확립 (S7)

| # | 결정 | 출처 |
|---|------|------|
| D49 | **analyst 2사이클 미수행의 원인은 태만이 아니라 「작업 공간 이탈」이었다.** analyst 세션의 cwd가 사용자 작업 루트가 아닌 **폐기 worktree**였고, 그 사본에서는 사용자 답변 칸·`HANDOFF` 갱신이 보이지 않았으며 analyst가 거기 쓴 결과도 사용자에게 보이지 않았다. **근본 원인은 S1(자기완결성) 자체다** — 모든 사본이 자기완결적이라 어느 사본에 있든 완전하고 권위 있어 보이고, **잘못된 사본에 있다는 신호가 존재하지 않는다.** 자기완결성이 치르는 대가이며, 따라서 짝이 되는 확인 규칙이 필요하다. 조치: (1) **S7 신설 「사본은 휘발된다 — 작업 루트는 하나」** — 세션 시작 시 자기 작업 디렉터리를 `SYNC.md`의 작업 루트와 대조하고, 다르면 **아무것도 쓰지 않고** 사용자에게 알린다. 다른 사본에 쓴 것은 **없는 것으로 간주**(S3와 동일 원리: 기록 위치가 틀리면 무효). (2) **작업 루트 대조를 `PROMPT` §7의 0번**으로 격상하고, 종료 전 "작업 루트 반영 확인"을 5번에 추가 — 임시 공간 작업 자체는 막지 않되 턴 종료 전 반영을 의무화한다. (3) **`SYNC.md`를 역사 → 운영 문서로 승격**하고 **director 단독 쓰기**로 지정. 작업 루트를 단일 선언하며 절대경로가 허용되는 유일한 파일 지위는 유지(S2). 그동안 소유자가 지정돼 있지 않았던 것도 함께 해소. (4) **사본 전수 조사 결과 4개 발견** — 기존 기록은 3개뿐이었다. worktree 2개(`…/.grok/worktrees/…/paper`, `…/paper-2`)는 **2026-07-21 상태로 폐지된 `To_Do.md`·`README_PATH.md`가 잔존**해, 거기서 지침을 읽으면 D40 이전 규칙을 현행으로 오인한다. `SYNC.md`에 전수 기재하고 삭제 여부를 사용자 미결로 올린다(P2: 규칙보다 구조 — 없는 사본에는 들어갈 수 없다). (5) 4개 진입점(`PROMPT`·`AGENTS`·`BOOTSTRAP_PROMPT`·`CLAUDE`) 전부에 0번 확인 반영. 판정: **analyst 승인** — `HO-20260724-01` 이행, D40–D48 파악, 원인 규명, `HO-20260724-04`의 9칸 오류를 스스로 발견해 "수정 전 소비 보류"로 판단한 것 모두 규격 충족. | 사용자 진단 (2026-07-24) + director 판정 |

## 2026-07-24 — 사본 단일화 · 채팅 키워드 `초기화` 신설

| # | 결정 | 출처 |
|---|------|------|
| D54 | **director 2차 `초기화` — 발산 3건 검출·시정. 사본 단일화 달성.** (1) **`SYNC.md`의 백업 경로가 실재하지 않았다.** `/home/user/fia_cm_haetae`가 통째로 휴지통으로 이동됐는데 목록은 여전히 "동결 백업"으로 선언하고 있었다. `SYNC.md`는 작업 루트 선언의 SSOT이므로 여기 적힌 사실이 틀리면 S7 전체의 신뢰가 무너진다. 결과적으로 **활성 사본 1개** — D50이 목표한 단일화가 실제로 달성됐다. 목록을 정정하고 **「이 목록 자체도 낡는다 — 매번 검색 명령으로 확인하라」**를 명시. (2) **`ROADMAP`의 「현재」·「다음」 섹션이 낡았다**(D42 시점 기재, DIL-12 미반영). 원인은 개별 오류가 아니라 D52에서 `MILESTONES` 「포인터」를 제거한 것과 **똑같은 구조적 결함** — 상태를 요약해 둔 중복이라 반드시 뒤처진다. 같은 처방을 적용해 **섹션째 제거**하고 SSOT 포인터로 대체(P1·P2). `ROADMAP`에는 **바뀌지 않는 계획**(목표·논문 순서·단계 흐름·원칙)만 남긴다. (3) **D53이 만든 규격 오류를 D53 적용 중에 자기 검출.** "응답 양식 **5항목** 중 **1번(작업 루트 대조 결과)**"이라고 썼으나, 실제 응답 양식 1번은 `role_id`이고 **작업 루트 대조는 양식에 아예 없었다**(독해 순서 0번에만 존재). 규칙을 처음 적용해 보자마자 드러났다. 시정: 응답 양식에 **0번 「작업 루트 대조 결과」를 추가**해 독해 순서 0번과 번호를 일치시키고, 참조를 `0–5번`으로 통일. **의의:** 세 발산 모두 기억으로는 보이지 않고 **폴더를 다시 읽어야만** 드러나는 것들이었다. `초기화`가 2회 연속 실제 결함을 잡아냈다(D52 2건 · D54 3건). | 사용자 `초기화` (2026-07-24) |
| D53 | **4역할 `초기화` 일제 실행 검수 — 로직 정상 동작 확인, 규격 결함 1건 시정.** 사용자가 `curator → analyst → producer → director` 순으로 `초기화`를 실행했다. **안전 확인(가장 중요):** 파일 총수 **141개 유지, 삭제 0건** — 「초기화」를 산출물 삭제로 오독한 역할이 하나도 없다. 4개 진입점에 "파일 삭제 명령이 아니다"를 명시한 D50 조치가 유효했다. **정합 확인:** 4역할의 재도출 답이 **모두 일치**했다 — 역할 바인딩(Codex/Grok/Agy/Claude), 소비 가능한 `ready`(curator `HO-20260724-05` · analyst `HO-20260723-11` · producer 없음), 유일한 차단(sparse-c-NTT). **소유 경계 위반 0건**(각자 자기 접점 파일만 수정), **handoff 소비 0건**(`HANDOFF.md` 무변경 — 「연구 미착수」 준수). 역할별: **curator 승인**(자기 대상 `ready`를 정확히 짚고 DIL-12 9칸 오류도 인지), **analyst 승인**(S7 대조 결과를 명시하고 `HO-20260724-04`의 status를 **열 기준**으로 정확히 분석해 소비 불가로 판정. 이전에 자기 파일에 넣었던 D40–D48 요약표를 `DECISIONS` 중복이라며 **자진 제거** — P1을 스스로 적용), **producer 조건부 승인**(재도출 자체는 정확하나 **작업 루트 대조 결과를 남기지 않아 확인 불가**). **결함은 producer가 아니라 director 규격이다** — `초기화` 후 접점 파일에 무엇을 남길지 정하지 않고 "`지금 상태`를 갱신한다"고만 해서, 세 역할이 서로 다른 수준으로 기록했다(analyst는 S7 명시, curator는 "작업 루트 일치", producer는 미기재). 시정: (1) **재구성 결과를 `BOOTSTRAP` 응답 양식 5항목 그대로** 접점 파일 `지금 상태`에 남기도록 규격화(P3 — 새 양식을 만들지 않고 기존 것 재사용). 특히 **작업 루트 대조 결과 필수** — 없으면 그 세션이 올바른 사본에 있었는지 사후 판정이 불가능하다. (2) "연구 작업 착수 금지"가 문서 위생 handoff까지 포함하는지 모호했으므로 **「초기화 중에는 어떤 handoff도 소비하지 않는다 — 목록 보고만」**으로 확정. 세 역할 모두 안전한 해석을 택했으나 규격에 못 박는다. | director 판정 (사용자 `초기화` 일제 실행 후 검수 지시) |
| D52 | **첫 `초기화` 실행 결과 — 자기완결성 시험 통과, 문서 모순 2건 검출.** director가 대화 기억을 버리고 폴더만으로 상태를 재도출했다. 재도출 자체는 성공(작업 루트 대조 `OK` → `README` → `PROMPT` §0 → `AI_ROSTER`로 `Claude=director` 확정 → `roles/director/` → `To_Do_Director` → `HANDOFF` 자기 대상). **검출된 모순:** (1) **`README` 디렉터리 트리가 `SYNC.md`를 「역사」로 표기** — 같은 문서의 운영 표(43행)는 "매 세션 제일 먼저"라고 하는데 트리(87행)는 「역사 = 읽지 않는다」로 분류했다. 트리를 따르면 **S7 대조를 아예 하지 않게 되어 D49 사고가 재발**한다. 트리에서 분리해 `★ 운영 · director 단독`으로 표기. (2) **`MILESTONES`의 「포인터」 섹션이 낡음** — "curator: DIL-10 자산 완료"(실제 DIL-12도 완료), "director: 미호출"(실제 D44–D51 다섯 차례 판정). 원인은 개별 오류가 아니라 **구조**다: 이 섹션은 `To_Do_*` `지금 상태` + `HANDOFF`를 요약한 중복이고(P1 위반), 갱신 주체가 불분명해 반드시 뒤처진다. 개별 행을 고치지 않고 **섹션째 제거**하고 SSOT 포인터 한 줄로 대체(P2 규칙보다 구조·P4 장식 금지). **추가 조치:** 사용자 지시로 Grok 활성 worktree 삭제 → **활성 사본 2개**(작업 루트 + 동결 백업). 삭제 전 확인: HEAD `19efcf7`가 본체에서 도달 가능, 변경분은 작업 루트의 현행 파일과 같은 계열. **결론:** `초기화`는 설계 의도대로 **자기완결성의 실행 시험**으로 기능했다 — 기억으로 덮여 보이지 않던 문서 모순이 기억을 버리자 드러났다. | 사용자 `초기화` (2026-07-24) |
| D51 | **사본 전수 조사 오류 정정 — 2개가 아니라 4개.** D50에서 "사본 4개 → 2개"로 보고했으나 재조사 결과 **4개**다. 원인: `find -name "Paper-Deep-Dive"`로 열거했는데 작업 루트 폴더명은 `[extra] Paper-Deep-Dive`라 **자기 자신조차 잡히지 않았다.** 경로 형태를 가정한 실수이며 D49 사고와 같은 종류다. 추가 발견: (a) **`/home/user/.grok/worktrees/user-chipwhisperer-kor/2026-07-24-214ec02b/workspace/[extra] Paper-Deep-Dive`** — 등록된 활성 worktree(detached `19efcf7`), 오늘 10:16 스냅숏. **D40+ 구조라 현행처럼 보이지만 S7 이전 지침**이라 작업 루트 대조를 하지 않는다. analyst가 다시 여기서 작업하면 D49 사고가 재발한다. (b) `/home/user/.local/share/Trash/files/[extra] Paper-Deep-Dive` — 2026-07-22, 이미 버려진 것. 확인된 사실: **S7의 보호는 사본 목록에 의존하지 않는다** — `pwd` 대 선언된 작업 루트 비교이므로 목록에 없는 사본에서도 `STOP`이 난다. **단 S7을 담고 있는 사본에서만 성립**하며, S7 이전에 동결된 사본은 무방비다 → **규칙이 아니라 삭제로만 막힌다**(P2). 조치: `SYNC.md`에 4개 전수 기재 + **폴더명이 아니라 내용 표식(`AI_ROSTER.md`)으로 찾는 명령** 수록. (a)의 처리는 활성 worktree 삭제가 analyst 세션에 영향을 주므로 사용자 미결로 올린다. | director 자기 검증 (2026-07-24) |
| D50 | **(1) 폐기 worktree 2개 삭제.** `…/.grok/worktrees/user-fia-cm-haetae/paper`·`paper-2`. D49 사고의 물리적 원인이며 2026-07-21 상태로 폐지된 `To_Do.md`·`README_PATH.md`를 담고 있어 읽는 것만으로 오염원이었다. 삭제 전 검증: 추적 콘텐츠 280여 개는 커밋 `1b810e4`로 복구 가능하고, **고유 파일 11개는 전부 이미 폐기된 것**(사용자가 직접 삭제한 `To_Do.md`·`README_PATH.md`·`.Prompt_Engineering/`, D24로 대체된 구 `Marp with LaTeX.css/` 툴킷, 파일명이 잘린 PCM-DFA PDF 중복본)이라 유실되는 현행 자료 없음. 근거: **없는 사본에는 들어갈 수 없다**(P2 규칙보다 구조 — S7 규칙만으로는 부족). **(2) 백업 사본 동기화 중단.** `fia_cm_haetae/Collabo_HB/Paper-Deep-Dive`를 2026-07-24 시점으로 **동결**하고 이후 갱신하지 않는다. 사본이 하나 더 있다는 것 자체가 D49 사고의 조건이었다. 동결 백업도 이 `SYNC.md`를 담고 있어 거기서 S7 대조를 실행하면 `STOP`이 나오므로 **스스로 보호된다**. 결과: 사본 **4개 → 2개**(작업 루트 + 동결 백업). **(3) 채팅 키워드 `초기화` 신설.** 정의: **이전 대화·세션 기억을 전부 버리고 폴더만으로 자기 상태를 처음부터 재구성한 뒤 보고**한다. 절차·응답 양식은 `BOOTSTRAP_PROMPT.md` 재사용(**재부트스트랩** — 새 절차를 만들지 않는다, P3). **파일을 지우거나 되돌리는 명령이 아니며** 초기화 대상은 AI의 인식뿐임을 모든 진입점에 명시(오독 시 산출물 삭제로 이어질 수 있는 위험 문구이므로). 쓰는 상황: AI 서비스 교체 · 새 PC 환경 구축 · 누적 이력 정리. 사용자는 모든 역할에 동시에 보낸다. **이 명령은 자기완결성의 실행 시험**이다 — 폴더가 정말 자기완결적이라면 기억을 버린 AI가 폴더만 읽고 같은 상태에 도달해야 하며, 어긋나면 그 자체가 결함이므로 보고한다(S6). 채팅 키워드는 이제 `계속`·`초기화` **둘뿐**이다. | 사용자 답변 칸 (2026-07-24) |

## 피드백 대기 큐

- 사용자 미결: **`To_Do_Analyst.md`** — sparse-c-NTT 한 줄 유지 여부
- DIL-10 자산은 `HO-20260723-11` ready; 분석은 사용자 게이트 후
- 사용자 미결: **`To_Do_Analyst.md`** — sparse-c-NTT (**유일한 전체 차단 원인**)
- 처리 대기: `HO-20260724-05` (→ curator, handoff 행 8칸 정정)
- **done**: `HO-20260724-01`(analyst) · `-02`(curator) · `-03`(producer)
- analyst 대기 중: `HO-20260723-11`(DIL-10, NTT 게이트 후 소비) · `HO-20260724-04`(DIL-12, 형식 정정 후 소비)
- 3역할 전원 승인 상태 — 남은 차단은 sparse-c-NTT 사용자 게이트 하나
- `To_Do_Curator.md` · `To_Do_Producer.md` · `To_Do_Director.md` 미결 없음
