# ROADMAP

갱신: 2026-07-24 · D54 — **계획 문서.** 현재 상태는 여기 두지 않는다

## 목표

승인 단위 심층 이해 → 근거 추적 가능 한글 Marp.

```text
M0 계약 → M-ARCH 자산 → H-M1–M3 분석 → H-M4–M6 Marp → H-M7 검증 → H-M8 최종
          curator        analyst         producer      director     사용자
                              └── 단계 종료마다 director 승인/반려 ──┘
```

`H-` 접두어는 논문별 마일스톤 (`MILESTONES.md`). `M0`·`M-ARCH`·`M-TRACE`는 논문 무관 전역 항목.

## 논문 순서

1. HAETAE-FIA
2. PCM-DFA (본문은 1 완료 후; 자료는 준비됨)

## 현재·다음

여기 두지 않는다 — **각 역할의 `To_Do_<Role>.md` `지금 상태`·`HANDOFF.md`·`MILESTONES.md`가 SSOT**다 (P1).

이전의 「현재」·「다음」 섹션은 그 셋을 요약해 두었다가 낡았다(2026-07-24 `초기화`에서 D42 시점 기재로 적발). `MILESTONES`의 「포인터」 섹션과 **같은 구조적 결함**이므로 같이 제거한다 (D54 · P2 규칙보다 구조).

이 문서에는 **바뀌지 않는 계획**만 둔다 — 목표 · 논문 순서 · 단계 흐름 · 원칙.

## 원칙

역할 밖 금지 · ready 전 소비 금지 · 추적 가능 · 자기 접점 파일 1판단 · 원본 무단 교체 금지 · 부족 시 분할 · 마일스톤 종료는 director 승인
