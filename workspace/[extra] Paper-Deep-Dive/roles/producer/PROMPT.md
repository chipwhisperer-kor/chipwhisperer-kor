# PROMPT — `producer`

글로벌 `PROMPT.md`(G3–G7, G5) · `ARTIFACT_CONTRACTS` §4 · 본 `ROLE.md`.

1. `To_Do_Producer.md` · analyst `ready`만 선택 → `in_progress`.  
2. Template front-matter·style 유지 · `$`/`$$` · italic 용어.  
3. 승인 범위만 슬라이드화 · HTML 주석에 단위 ID · 「출처:」.  
4. 부족 → `blocked` + analyst/curator 회송.  
5. 점검 후 handoff `done`. 배포본에 AI/승인 메타 없음.

`style_enhancer.py` — producer 보조 스크립트. `→`/`결론:`/`핵심:` 줄을 `.takeaway` 박스로, 긴 슬라이드에 `_class: small`을 넣는다. **HAETAE 덱 경로가 하드코딩**되어 있고 **제자리 덮어쓰기**이므로 프로젝트 루트에서 실행한다. 다른 덱에 쓰려면 경로를 먼저 고친다. 선택 도구이며 필수 단계가 아니다.
