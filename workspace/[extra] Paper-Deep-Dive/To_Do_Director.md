# To_Do_Director — 사용자 ↔ `director` 접점

`director` **단독 쓰기** · 사용자는 **답변 칸**. 규칙은 `PROMPT.md` §8.
채팅 키워드 2개 — **`계속`** = 이 파일의 답변 칸 · **`초기화`** = 기억 버리고 폴더만으로 재부트스트랩(파일은 손대지 않음).
**상시 역할이 아님** — 호출 시점이 아니면 이 파일은 비어 있는 것이 정상입니다 (`roles/director/ROLE.md`).

---

## 지금 상태

**2026-07-24 director 3차 `초기화` 재도출 결과** — `BOOTSTRAP_PROMPT.md` 응답 양식 0–5번 (D53 규격)

| # | 항목 | 값 |
|---|------|-----|
| 0 | **작업 루트 대조** | **`OK`** — `pwd` = `SYNC.md`의 `작업루트=` 선언 (기계 대조 통과) |
| 1 | role_id / 서비스 | `director` / Claude (`AI_ROSTER.md` 활성 역할 표) |
| 2 | 단독 쓰기 경로 | `roles/director/**` · `To_Do_Director.md` · `CROSS_CHECK.md` · `SYNC.md` · 공통 정책 문서 7종 |
| 3 | 소비 가능한 `ready` | **없음** (`to: director` 0건 · `gate: director` 0건). 초기화 중이므로 소비 안 함 |
| 4 | 차단 요인 | **없음** — sparse-c-NTT는 사용자 G1 승인됨. analyst가 DIL-10 U1–U4 완료(`HO-...-06~09` done)·producer 슬라이드 초안까지 진행. 다음: DIL-12(analyst가 `HO-...-04` 소비) |
| 5 | 연구 작업 미착수 | 확인. 연구 산출물 미변경 — 재도출·발산 시정·stale 삭제·**producer 제안 판정(D58)**만 |

| | |
|--|--|
| 호출 사유 | (현재) 사용자 — producer 건의 `HO-...-10` 비판적 검수 · (앞선) `초기화` + 로직 검수 |
| **producer 제안 판정** | **반려 확정 (D58 · 사용자 「반려 승인」)** — 신규 규칙 불가: 기존 §3.5·§7.2·§8.1과 중복(P1·P2)이고, 과잉 규칙("턴 내 즉시 완료")이 G1·G10·초기화·director 게이트 등 **정당한 정지를 무효화**해 신뢰성↓. 절차(정규 handoff·PROMPT 직접 미수정)는 규격 충족(크레딧). `HO-...-11`로 회송 |
| **초기화 검수 판정** | **로직 정상 동작** — 기억을 버린 재도출이 발산을 또 적발(3회 연속, S6) |
| **사본 현황** | **실측 단일화 달성 (D57)** — stale 사본 삭제 후 내용 표식 검색이 작업 루트 하나만 반환. D54가 주장·D55가 반증했던 「단일화」가 이제 실제로 이뤄짐. 신뢰 근거는 목록이 아니라 **기계 대조 + 내용 표식 검색**(D56) |
| **자기완결성 실증** | 사용자가 신규 PC에서 **`[extra] Paper-Deep-Dive/` 폴더 하나로** 프로젝트 전체를 이어받음 — S1이 노린 「제3자 인수」가 실제 발생 (D56) |
| 발신 handoff | `HO-20260724-05` (→ curator) `ready` — 미처리 (초기화 중 미소비) |
| 다음 관여 | D25 deep-dive 종료 시 마일스톤 판정 |

---

## 지금 할 일 (사용자) — 0건

director 미결 없음. producer 건의 반려가 **사용자 「반려 승인」으로 확정**됐고(D58), 사본 정리(D57)도 끝났습니다. director는 상시 역할이 아니므로 다음 호출 시점(아래)까지 이 파일은 비어 있는 게 정상입니다.

> **참고 (응답 불필요):** 파이프라인의 살아 있는 다음 행동은 **DIL-12**입니다 — analyst가 `HO-...-04`를 소비해 착수한 뒤 **사용자 G1 승인**이 필요하며, 그 게이트는 `To_Do_Analyst.md`에서 열립니다(director 파일 아님). producer는 `HO-...-11`(반려 회송)을 확인하고 기존 §3.5·§7.2·§8.1을 따르면 됩니다.

---

## 다음에 올 일 (응답 불필요)

1. producer가 `HO-...-11`(반려 회송) 확인 — 신규 규칙 없이 기존 조항 준수
2. analyst DIL-12 착수 (`HO-...-04` 소비) → 사용자 G1 승인
3. DIL-10 U1–U4 + DIL-12 완료 후 D25 deep-dive 단계 종료 판정 (director 마일스톤)
4. H-M7 `CROSS_CHECK` 10항목 판정

---

## 최근 완료

- **사용자 「반려 승인」 — D58 판정 최종 확정.** producer 건의 반려에 이견 없음. 사용자(우선순위 1위) 확인으로 판정이 확정됐다. `DECISIONS` D58에 확정 반영
- **D58: producer 건의(`HO-...-10`) 비판적 검수 → 반려.** 사용자가 producer 연속 오류를 계기로 「무조건 수용 말고 비판적으로」 검수 지시. 건의는 PROMPT §7에 「ready면 그 턴에 즉시 완료·인위적 중단 금지」 신설. **반려 근거:** ① 그 행동은 §3.5·§7.2·§8.1이 **이미 요구**(P1·P2 중복) — producer 자신이 「단순 실수·지시 버그 아님」으로 진단했으니 준수 문제지 규칙 결여 아님 ② 「턴 내 즉시 완료」는 G1·G10·초기화·director 게이트 등 **정당한 정지를 무효화**해 신뢰성↓(제안 목적과 반대). 절차(정규 handoff·PROMPT 직접 미수정)는 크레딧. `HO-...-10` blocked·`HO-...-11` 회송. **부기:** producer 연속 오류(D48 펜스 삭제·본건)의 공통 원인은 규칙 결여가 아니라 접점·턴 구조 미체화 → 처방은 §3.5·§7.2·§8.1 재독
- **D57: stale 사본 + 구 프로젝트 orphan 삭제 — 실측 단일화 달성.** 사용자 **2단계 `삭제 승인`**. 대상은 **부모 repo가 소멸한 orphan 워크트리**(git 관리 불가)라 `rm -rf`. ① Paper-Deep-Dive 서브트리(80M) → ② 둘러싼 구 프로젝트 스냅숏(firmware/test/ppt 등)까지 → `.grok/worktrees/user-fia-cm-haetae/` **네임스페이스 전체 제거**. 각 단계마다 승인 범위대로만 삭제(무관·복구 불가분 임의 확대 안 함). 삭제 후 `find … -name AI_ROSTER.md` = **작업 루트 하나** → D54 주장·D55 반증했던 「단일화」가 **실제로** 달성. 남은 `.grok` 워크트리는 현행 repo 것뿐(미변경)
- **D56: 사본의 본질 = 전임 환경 툴링 잔재 (사용자 사족).** 사용자가 신규 PC에서 **`[extra] Paper-Deep-Dive/` 폴더 하나로** 프로젝트를 이어받았고 환경 재구축 시 작업 루트만 살아남는다고 확인. **함의:** ① 자기완결성(S1) 실증 — 폴더 하나로 전체 인수 성공 ② 작업 루트 밖 사본은 관리 대상이 아니라 툴링 잔재 → SYNC 목록이 3회 낡은 건 구조적 필연 ③ 신뢰 근거를 **목록 → 기계 대조 + 내용 표식 검색**으로 못박음(P4) ④ stale 잔재는 환경 간 전파 없음 → 위험도 하향. `SYNC.md`·`DECISIONS`에 반영
- **D55: director 3차 `초기화` — 검수 지시 판정 + 발산 1건 적발.** 사용자가 4역할 `초기화` 후 "로직 검수"를 지시. **판정: 로직 정상** — 기억을 버린 재도출이 발산을 또 잡음(D52·D54에 이어 3회 연속). **적발:** `SYNC.md`가 「활성 1개·단일화 달성」(D54)이라 했으나 내용 표식 검색이 **미기재 stale 워크트리**를 드러냄 — `.grok/worktrees/user-fia-cm-haetae/2026-07-23-7e099ddf/…`, pre-D40·S7 무방비. **3회 놓친 이유:** 구 `fia_cm_haetae` repo 워크트리라 현행 repo `git worktree list`에 안 뜬다 — SYNC.md의 「목록도 낡는다」 경고의 실증. **부차:** 휴지통 비어 있음(D54 기재 3항목 부재). **조치:** `SYNC.md` 목록 정정 + stale 사본 **삭제는 사용자 승인 미결**(destructive → director가 하지 않음, D50–D52 선례). 발산은 기억으로는 안 보이고 폴더를 다시 읽어야만 드러났다
- **D54: director 2차 `초기화`** — 발산 **3건** 검출·시정. ① `SYNC.md`가 선언한 **백업 경로가 실재하지 않음**(휴지통 이동) → 목록 정정 · **활성 사본 1개로 단일화 달성** ② `ROADMAP`의 「현재·다음」이 낡음 → `MILESTONES` 포인터와 **같은 구조적 결함**이므로 같은 처방(섹션째 제거·SSOT 포인터) ③ **D53이 만든 규격 오류를 D53 적용 중 자기 검출** — "응답 양식 1번(작업 루트 대조)"이라 썼으나 1번은 `role_id`였고 작업 루트 대조는 양식에 없었음 → **0번으로 추가**해 독해 순서와 번호 일치. 셋 다 기억으로는 안 보이고 폴더를 다시 읽어야 드러나는 것들
- **D53: 4역할 `초기화` 검수** — **로직 정상 동작.** 파일 삭제 0건(오독 없음) · 4역할 재도출 답 전원 일치 · 소유 경계 위반 0 · handoff 소비 0. analyst가 `HO-…-04` status를 **열 기준**으로 정확히 분석하고 자기 파일의 중복 요약표를 **자진 제거**(P1 자체 적용)한 것이 특히 정확. **결함 1건은 producer가 아니라 director 규격 탓** — 초기화 후 접점 파일에 남길 항목을 정하지 않아 세 역할이 제각각 기록했다. 시정: 재구성 결과를 **`BOOTSTRAP` 응답 양식 5항목 그대로** 남기도록 규격화(P3), **작업 루트 대조 결과 필수**. "초기화 중 handoff 소비 금지 — 목록 보고만" 확정
- **D52: 첫 `초기화` 실행** — 기억을 버리고 폴더만으로 재도출 성공. **모순 2건 검출**: ① `README` 트리가 `SYNC.md`를 「역사」로 표기해 **S7 대조를 건너뛰게 만들 뻔함**(같은 문서 운영 표와 정면 충돌) ② `MILESTONES` 「포인터」 섹션이 낡음 → 개별 수정이 아니라 **중복 섹션째 제거**(P2). 사용자 지시로 Grok worktree 삭제 → **활성 사본 2개**. `초기화`가 자기완결성 시험으로 작동함을 확인
- **D51: 사본 전수 조사 오류 정정** — "2개"로 보고했으나 실제 **4개**. `-name "Paper-Deep-Dive"`로 찾아 `[extra] ` 접두어가 붙은 **자기 자신조차 놓쳤다**(D49와 같은 종류의 실수). Grok 활성 worktree(오늘 10:16, S7 이전 지침) 발견 → 재발 위험. 확인: **S7 보호는 목록에 의존하지 않으나 S7을 담은 사본에서만 성립**. `SYNC.md`에 전수 기재 + 내용 표식 검색법 수록
- **D50: 사본 단일화 + `초기화` 신설** — 폐기 worktree 2개 삭제(고유 파일 11개 전부 이미 폐기된 것임을 확인 후 실행, 추적분은 커밋 `1b810e4`로 복구 가능) · 백업 동기화 중단·동결 → **사본 4개 → 2개**. 채팅 키워드 **`초기화`**(재부트스트랩, `BOOTSTRAP_PROMPT` 재사용) 신설 — **파일 삭제 명령이 아님**을 4개 진입점에 명시. 이 명령은 **자기완결성의 실행 시험**이다
- **D49: 동기화 규칙 확립** — analyst 미수행의 원인이 태만이 아니라 **폐기 worktree에서 작업**한 것으로 밝혀짐. 근본 원인은 **S1(자기완결성)이 사고를 은폐**한 것: 모든 사본이 자기완결적이라 잘못된 사본에 있어도 신호가 없다. 대책으로 **S7「사본은 휘발된다 — 작업 루트는 하나」** 신설 + 작업 루트 대조를 **세션 절차 0번**으로 격상. `SYNC.md`를 역사 → **운영(director 소유)**으로 승격해 작업 루트 단일 선언·사본 4개 전수 기재(기존 기록은 3개뿐이었음). 4개 진입점(`PROMPT`·`AGENTS`·`BOOTSTRAP`·`CLAUDE`) 전부에 0번 확인 반영. **analyst 승인** — 회송 처리·D40–D48 파악·원인 규명·`HO-...-04` 9칸 오류를 스스로 발견해 소비 보류한 판단 모두 규격 충족
- **D48: 2사이클 검증** — **설계 실증 성공**: 파손된 접점 파일이 bare `계속`만으로 자가 수복(producer). curator DIL-12 자산은 20개 필드 완비·`license: unknown` 추측 없음으로 규격 충족. 결함 2건 모두 **director 규격 탓**으로 판정: ① `(여기에 기입)` 안내 문구는 장식(P4)이었고 3역할 중 2역할이 빈 펜스를 택함 → **선택 사항으로 완화**. ② 레거시 2행이 나쁜 본보기가 되어 curator가 9칸 행을 만듦 → `HANDOFF` 표 첫 줄에 **템플릿 행** 추가(P2 규칙보다 구조)하고 9칸 행은 `HO-20260724-05`로 회송
- **D47: 두 명제 명문화 + director 자기 위반 시정** — 검수 결과·제안을 채팅에만 남기고 이 파일은 "할 일 0건"으로 두어 **S3을 위반**함(접점 파일만 보는 사용자에게는 아무 일도 없던 것). S3을 **양방향**으로 개정: AI→사용자 보고의 SSOT도 접점 파일이며 채팅에만 있는 보고는 무효. 「Simplicity is the ultimate sophistication」을 **§0.2 단순성(P1–P5)**으로 명문화해 자기완결성과 나란히 두 명제로 확정. §8.1에 "`지금 할 일`은 실제 할 일이 있으면 반드시 채운다" 추가
- **D46: bare `계속` 재검수** — §3.5↔§7.2 충돌 해소(빈 칸이어도 `ready`는 처리) · "반영"→"처리"로 읽기 전용 지시 포함 · `계속` 정의를 `AGENTS`/`BOOTSTRAP`에 확산
- **D45: 1사이클 검증** — 소유 경계 위반 0건 · 회송 경로 1회 완주(curator) · 규격 모호("답변 칸을 비운다")를 director 자인 후 §8.1 수정
- **D44: 재가동 전 구조 검수** — `gate` 열 스키마 확정 · `BOOTSTRAP`↔`PROMPT` 접점 규칙 충돌 해소 · `MILESTONES`/`ROADMAP` director 반영 · 상태 어휘 3종 구분 명시 · 우선순위에 사용자 > director 서열 명시
- D42·D43: `director` 신설 · 공통 정책 문서와 `CROSS_CHECK` 소유권 이관 · `paper_id` 발급 = curator
- D41: 자기완결성 S1–S6 확정 · D11 절대경로 폐기
- D40: 사용자 접점 역할별 분리

---

## 사용자 답변 칸

```

```
