"""단순 PRE-SCA 스크립트의 공용 설정.

경로는 이 파일의 위치를 기준으로 계산하여 실행 디렉터리에 의존하지 않는다.
이 모듈을 import하는 다른 모듈이 아래 식별자를 이름으로 참조하므로 변수명은 공개
인터페이스로 취급한다. import 시 외부 파일을 읽거나 쓰지 않는다.
"""

import os

# -----------------------------------------------------------------------------
# 경로 설정
# -----------------------------------------------------------------------------
# 현재 파일(config.py)의 위치를 기준으로 프로젝트 루트 디렉터리를 계산합니다.
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# -----------------------------------------------------------------------------
# 공용 설정값
# -----------------------------------------------------------------------------
# 주의: 아래 변수명들은 elfParser, emul, logger 모듈에서 직접 참조하므로
# 리팩토링 시 이름을 변경하지 마십시오.

log_file_name = "tiny-AES_rand"

# 절대경로. 호환성을 위해 변수명은 snake_case를 유지합니다.
elf_file = os.path.join(BASE_DIR, "source", "tiny-aes")
log_vir_in_file = os.path.join(BASE_DIR, "ini_set", "LogVirIN.csv")
fault_reg_file = os.path.join(BASE_DIR, "ini_set", "LogFI.csv")

# 에뮬레이션 매개변수
BUFFER_BLOCK = 16   # 입력 데이터 블록 크기(바이트)
BUFFER_NUM = 10     # 처리할 블록(행)의 개수
