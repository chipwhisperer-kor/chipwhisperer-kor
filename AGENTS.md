# AGENTS.md

이 저장소에서 작업하는 모든 AI 에이전트는 아래 **3원칙**을 따른다.

3원칙은 지침이 아니라 **판단 기준** 이다. 이 문서에 명시되지 않은 상황을 만나면 임의로 결정하지 말고, 3원칙에 비추어 판단한다.
번호는 **작업이 진행되는 순서**를 따른 것이며, **우선순위나 서열이 아니다.** 원칙끼리 충돌할 때는 순번이 아니라 문서 끝의 기준으로 판단한다.
이 파일은 프로젝트 생성 시 기본 제공되는 공통 기준이며, 언어·프레임워크·스택에 무관하게 적용된다. 이 파일에 프로젝트별 규칙을 덧붙이더라도 3원칙이 항상 상위 기준이다.

---

## 원칙 1 — 단일 진실 공급원 (Single Source of Truth)

> **이 프로젝트에 관한 사실은 실제 파일 안에만 존재한다.** 기억·추측·이전 대화 내용은 사실의 근거가 되지 않는다.
> 따라서 무엇이 사실인지는 항상 실제 파일을 읽어 확인하고, 하나의 정의(값·설정·구조)는 오직 한 파일에만 둔다.

**모든 작업은 여기서 시작한다.** 무엇이 있는지 확인하기 전에는, 무엇이 불필요한지(원칙 2)도 무엇을 설명해야 하는지(원칙 3)도 판단할 수 없다.

이 원칙은 다음 두 가지를 동시에 요구한다.

### 1-1. 확인은 반드시 실제 파일로 한다 (Verify, don't assume)

- 작업 전에 관련 파일을 먼저 읽는다. 읽지 않은 파일의 내용에 대해서는 어떤 가정도 하지 않는다.
- 파일 경로, 함수 시그니처, 설정 키 이름, 의존성 버전은 사용하기 전에 해당 파일에서 읽어 확인한다.
- 확인할 수 없는 것은 지어내지 않는다. 확인하지 못했다는 사실을 명시하고 사용자에게 질문한다.
- 작업을 마친 뒤에는 변경된 파일의 실제 상태를 다시 읽어 결과를 확인한다. "적용되었을 것이다"로 보고하지 않는다.

**자가 점검 (Self-check)**
> "이것을 나는 파일에서 읽어 확인했는가, 아니면 안다고 생각하고 있는가?"

### 1-2. 같은 정보를 두 곳에 기록하지 않는다 (Define once)

- 설정값·상수·타입·스키마·경로는 한 곳에서만 정의하고, 나머지 위치에서는 그것을 참조(import)한다.
- 같은 값을 다시 적어야 하는 상황이 생기면, 그것은 복사할 신호가 아니라 **정의를 한 곳으로 모을 신호**다.
- 이미 두 곳 이상에 같은 값이 정의되어 있는 것을 발견하면 임의로 정리하지 말고, 어느 쪽이 정본(canonical)인지 사용자에게 확인한다.

**자가 점검 (Self-check)**
> "이 값을 바꿔야 할 때, 고쳐야 할 곳이 두 군데 이상인가?"
> 그렇다면 설계가 잘못된 것이다.

---

## 원칙 2 — 단순함이 궁극의 정교함이다 (Simplicity is the Ultimate Sophistication)

> 문제가 복잡하더라도, 먼저 그 본질을 완전히 이해한다.
> 그 다음, 본질에 필요하지 않은 것을 모두 덜어낸다. 그렇게 남은 것이 명확하고 견고한 설계다.
> (KISS: Keep It Simple, Stupid)

**순서가 핵심이다.** 이해하지 않은 채 덜어낸 결과물은 단순함이 아니라 미완성이다. 적게 만드는 것과 덜 이해하는 것은 다르다.

**지킬 것 (Do)**
- 요구된 문제만 해결한다. 요청되지 않은 기능·옵션·설정은 만들지 않는다. (YAGNI: You Aren't Gonna Need It)
- 동작이 동일하다면 항상 더 적은 코드, 더 적은 파일, 더 적은 의존성을 선택한다.
- 추상화는 실제 중복이 **3회 이상** 관측된 뒤에 도입한다. 2회는 우연의 일치일 수 있지만 3회부터는 패턴으로 확정되기 때문이다. (Rule of Three) "나중에 필요할 것 같아서"는 근거가 아니다.
- 작업 결과 불필요해진 코드는 삭제한다. 주석 처리로 남기지 않는다.

**하지 말 것 (Don't)**
- 아직 존재하지 않는 요구사항을 위한 선제적 확장 지점(hook, plugin, config flag) 설계
- 한 곳에서만 쓰이는 래퍼(wrapper)·인터페이스·헬퍼 계층
- 요청받지 않았고 바꿀 이유도 없는 코드에 대한 리팩터링

**자가 점검 (Self-check)**
> "이 요소를 지우면 무엇이 깨지는가?"
> 답하지 못하면, 지운다.

---

## 원칙 3 — 자기완결적 문서·주석 (Self-Contained Documentation)

> 외부 문서나 다른 코드 조각을 찾아보지 않고도, 그 문서(또는 주석) 하나만으로 해당 코드의 의미와 사용법을 완전히 이해할 수 있어야 한다.

기준 독자는 **이 프로젝트를 처음 보는 사람 또는 AI**다. 배경 지식도, 이전 대화 맥락도 전혀 없다고 가정한다.

**지킬 것 (Do)**
- 코드가 이미 말하는 것(what)이 아니라, 코드가 말할 수 없는 것(why)을 쓴다: 왜 이 방식인가, 어떤 대안을 왜 버렸는가, 어떤 제약 때문인가.
- 함수·모듈 설명에는 목적, 입력, 출력, 실패 조건(failure mode), 부작용(side effect)을 포함한다.
- 자명하지 않은 값(타임아웃, 재시도 횟수, 임계치 등)에는 그 값이어야 하는 이유를 함께 적는다.
- 코드를 수정하면, 그 코드를 설명하는 문서·주석을 **같은 작업 안에서** 함께 수정한다.

**하지 말 것 (Don't)**
- "자세한 내용은 ○○ 참조", "위 함수와 동일" 처럼 이해를 다른 곳으로 미루는 서술
- 링크나 경로만 남기고 핵심 내용을 옮겨 적지 않는 것 — 링크는 보조 자료이며, 이해에 필요한 내용은 본문에 있어야 한다
- 지시 사항이나 대화 이력을 안다고 전제하는 표현 ("요청하신 대로", "앞서 논의한 방식으로")

**자가 점검 (Self-check)**
> "이 설명만 읽고, 다른 파일을 한 번도 열지 않고도 이 코드를 올바르게 사용할 수 있는가?"

---

## 원칙이 충돌할 때 (Conflict Resolution)

원칙끼리 충돌하는 것처럼 보인다면, 번호 순서로 판단하지 말고 각 원칙이 적용되는 **대상**을 구분한다.

| 충돌 상황 | 판단 |
| --- | --- |
| **원칙 1 vs 원칙 3**<br>자기완결적으로 쓰려면 다른 곳의 내용을 옮겨 적게 된다 | **의미는 반복해도 되지만, 정의는 반복하지 않는다.** 그 값이 무엇을 위한 것이고 어떤 제약이 있는지는 설명에 온전히 쓰되, 값 자체는 정의된 한 곳에서만 관리하고 참조한다. |
| **원칙 2 vs 원칙 3**<br>설명을 충실히 쓰면 분량이 늘어난다 | 원칙 2는 **구조(코드·파일·의존성)** 에, 원칙 3은 **설명(문서·주석)** 에 적용된다. 코드는 최대한 덜어내되, 설명은 이해에 필요한 만큼 쓴다. 짧게 만들려고 이유(why)를 빼지 않는다. |
| **판단이 서지 않을 때** | 추측으로 진행하지 않는다. 가능한 선택지와 각각의 판단 근거를 제시하고 사용자에게 확인한 뒤 진행한다. |
