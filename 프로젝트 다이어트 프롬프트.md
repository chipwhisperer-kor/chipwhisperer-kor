# 프로젝트 다이어트 프롬프트

---

## A. 압축본

```markdown
## 프로젝트 다이어트 원칙

이 코드베이스는 창고가 아니라 작업대다. "언젠가 쓸지도 모른다"고 남긴 코드는
집안에 쌓아둔 쓰레기와 같다. 보관은 공짜가 아니며, 남은 한 줄은 이후 모든
세션의 모든 독자에게 반복해서 비용을 청구한다. 비운 만큼 채울 수 있다.

- 지운 것은 사라지지 않는다. git이 전부 보관한다. "혹시 몰라서" 남기지 마라.
- 다이어트 ≠ 축소. 관측 가능한 동작과 성능은 그대로 두고 표면적만 줄인다.
  기능 제거·에러 처리 생략·테스트 삭제로 줄이는 것은 다이어트가 아니라 절단이다.
- 삭제 우선순위 = 코드량 × 읽히는 빈도. 매 세션 로드되는 진입점·공용 유틸·
  루트 문서의 100줄이, 아무도 안 여는 폴더의 1000줄보다 비싸다.
- 주석 처리·`_old` 접미사·`archive/` 폴더로 옮기기 금지. 지울 거면 지워라.
- 확신(참조 0건 확인) → 즉시 삭제. 의심 → 근거와 함께 사용자에게 질문.
  모르면 남긴다. "무엇이 깨지는지 모르는 것"은 아직 지울 준비가 안 된 것이다.
- 삭제 커밋과 기능 변경 커밋을 섞지 마라. 삭제 후 빌드·타입체크·테스트로 검증.
- 작업이 끝나면 항상 새 코드가 만든 흔적(임시 로그, 일회용 헬퍼, 죽은 분기)을
  같이 회수한다. 다이어트는 이벤트가 아니라 습관이다.
```

---

## B. 전체본

```markdown
# 프로젝트 다이어트 지침

## 1. 철학

이 코드베이스는 창고가 아니라 작업대다.

"언젠가 쓸지도 모른다"는 이유로 남겨둔 코드는, 현실에서 집안에 쓰레기를 쌓아두는
행위와 정확히 같다. 버리지 않은 물건은 공짜로 보관되지 않는다. 매번 시야에 들어오고,
매번 "이게 뭐였더라"를 판단하게 만들고, 정작 필요한 물건을 찾는 시간을 늘린다.
코드도 똑같다. 죽은 코드 한 줄은 삭제되기 전까지, 이 프로젝트를 읽는 모든 사람과
모든 AI 세션에게 이자를 붙여 비용을 청구한다.

비운 만큼 채울 수 있다. 삭제는 잃는 행위가 아니라 자리를 만드는 행위다.

**그리고 지운 것은 사라지지 않는다.** 버전 관리 시스템이 모든 과거를 보관하고 있다.
"혹시 몰라서" 남기는 사본, 주석 처리한 블록, `_old` 접미사는 git이 이미 더 잘 하고
있는 일을 훨씬 나쁜 방식으로 중복하는 것이다. 되살릴 확률은 낮고, 되살리는 비용은
`git log`와 `git revert` 한 번이다.

## 2. 왜 지금 더 중요한가 — 컨텍스트는 예산이다

이제 이 프로젝트를 가장 자주, 가장 처음부터 끝까지 읽는 독자는 AI다.
따라서 프로젝트의 크기는 미학의 문제가 아니라 **비용과 정확도의 문제**다.

- 컨텍스트에 올라간 모든 토큰은 지불된 비용이다.
- 컨텍스트가 비대해질수록 모델은 목표를 잃는다. 지시의 우선순위가 흐려지고,
  중간 정보가 희석되고, 엉뚱한 파일을 근거로 판단한다.
  **과도한 컨텍스트는 AI가 목표를 상실하는 가장 큰 단일 요인이다.**
- 죽은 코드는 단순히 자리만 차지하지 않는다. 검색 결과를 오염시키고, 잘못된 패턴을
  모방 대상으로 제시하고, "이 함수와 저 함수 중 무엇이 진짜인가"라는 판단을 매번 강요한다.

그러므로 삭제 우선순위는 다음과 같이 매긴다:

> **우선순위 = 코드량 × 읽히는 빈도**

매 세션 로드되는 진입점, 공용 유틸리티, 타입 정의, 루트 문서(`README.md`,
`CLAUDE.md`, `AGENTS.md`)의 100줄은, 아무도 열지 않는 하위 폴더의 1000줄보다 비싸다.
**이 지침 문서 자체와 프로젝트 규칙 문서도 다이어트 대상이다.**

## 3. 절대 오해하지 말 것: 다이어트 ≠ 축소

세 문장으로 요약한다.
- **기능은 그대로 둔다.** 사용자가 관측할 수 있는 동작은 1%도 바뀌면 안 된다.
- **성능은 그대로 두거나 개선한다.** 캐싱, 최적화, 방어 로직을 "복잡하다"는 이유로 걷어내지 않는다.
- **줄이는 것은 표면적이다.** 읽어야 할 양, 이해해야 할 개념 수, 중복된 진입 경로.

기능 제거·에러 처리 생략·테스트 삭제·검증 로직 단순화로 라인 수를 줄이는 것은
다이어트가 아니라 **절단**이다. 라인 수는 목표가 아니라 결과 지표다.
줄일 수 없으면 줄이지 않는 것이 맞다.

## 4. 지방 — 적극적으로 제거할 것

- **죽은 코드**: 어디서도 참조되지 않는 함수·클래스·컴포넌트·파일·export·타입
- **주석 처리된 코드 블록**: 예외 없음. git에 있다.
- **버전 사본**: `foo_v2.ts`, `foo.old.js`, `utils_backup/`, `legacy/`, `archive/`
- **무분별한 헬퍼**: 같은 일을 하는 유틸이 3개 → 1개로 통합. 한 곳에서만 쓰이는
  래퍼 함수는 호출부로 인라인.
- **작업 잔여물 로그**: 디버깅용 `console.log`, `print`, 주석 `// TODO: 나중에 지울 것`,
  임시 플래그, 실험용 분기
- **과잉 추상화**: 구현체가 하나뿐인 인터페이스, 아무도 갈아끼우지 않는 어댑터,
  설정값이 한 번도 바뀐 적 없는 옵션 플래그
- **도달 불가능한 분기**: 상위에서 이미 걸러지는 조건, 죽은 feature flag의 반대편
- **미사용 의존성·설정**: `package.json` / `requirements.txt`의 안 쓰는 패키지,
  참조되지 않는 환경변수, 죽은 CI 스텝
- **사문화된 문서**: 실제 코드와 어긋난 README 섹션, 이미 끝난 마이그레이션 가이드,
  1년 지난 TODO, 다시 열리지 않을 설계 메모
- **일회용 스크립트**: 이미 실행 완료된 마이그레이션·시드·백필 스크립트

## 5. 근육과 뼈 — 건드리지 말 것

"안 쓰는 것처럼 보인다"와 "안 쓴다"는 완전히 다르다. 다음은 정적 검색으로 참조가
잡히지 않으므로, 참조 0건을 삭제 근거로 삼지 마라.

- **동적 참조**: 리플렉션, DI 컨테이너 등록, 문자열 기반 라우팅, 플러그인 로더,
  `import()` 동적 임포트, i18n 키, 템플릿/설정 파일에서 문자열로 지목되는 심볼
- **외부 계약**: 라이브러리 공개 API, 다른 레포가 소비하는 export, 웹훅 핸들러,
  CLI 엔트리, DB 마이그레이션 이력
- **테스트 코드**: 커버리지는 자산이다. 테스트를 지워 빌드를 통과시키는 것은 최악의 행위.
- **에러 처리와 경계 조건**: 평소에 실행되지 않는 것이 정상인 코드다. "안 타는 분기"는
  삭제 사유가 아니다.
- **운영 관측성**: 감사 로그, 메트릭, 에러 리포팅. (임시 디버그 로그와 반드시 구분할 것)
- **보안·라이선스·규정 관련 코드와 고지**
- **최근 추가분**: 최근 커밋으로 들어온 코드는 아직 연결 중일 수 있다. 삭제 전 확인.

## 6. 작업 절차

1. **측정** — 삭제 후보를 나열하고 각각에 근거를 붙인다. 전체 참조 검색, 동적 참조
   패턴 검색, `git log`상 최종 수정 시점, 테스트 커버리지.
2. **분류** — 세 등급으로 나눈다.
   - **확실**: 참조 0건 + 동적 참조 가능성 없음 + 외부 계약 아님 → 삭제 진행
   - **의심**: 근거는 있으나 확신 부족 → 사용자에게 질문 (`X를 지우려 합니다.
     근거는 A·B, 걸리는 점은 C입니다. 진행할까요?`)
   - **보류**: 판단 불가 → 남기고 목록에만 기록
3. **실행** — 작은 단위로 나눠 삭제한다. **삭제 커밋과 기능 변경 커밋을 절대 섞지 않는다.**
   리뷰어(그리고 미래의 AI)가 "이 커밋은 동작을 바꾸지 않는다"를 한눈에 믿을 수 있어야 한다.
4. **검증** — 매 단계마다 빌드, 타입체크, 린트, 테스트를 통과시킨다. 실패하면 즉시 되돌린다.
5. **보고** — 아래 형식으로 정리한다.

## 7. 금지된 습관

- 삭제 대신 주석 처리
- `deprecated_`, `_old`, `_backup`, `legacy_` 접두·접미사 붙여 존치
- `archive/`, `trash/`, `_unused/` 폴더로 이동 (프로젝트 안에 있으면 여전히 읽힌다)
- 삭제 커밋에 리팩터링·기능 변경을 끼워 넣기
- 테스트를 지워서 실패를 없애기
- 사용자에게 묻지 않고 애매한 것을 삭제하기
- 라인 수를 줄이려고 가독성을 희생하기 (한 줄짜리 난해한 표현식으로 압축하는 행위)

## 8. 판단 기준

애매할 때 다음을 계산한다.

> **남기는 비용** = (읽는 비용 × 앞으로 읽힐 횟수)
> **지우는 비용** = (다시 필요해질 확률 × git에서 되살리는 비용)

`git revert`가 존재하는 한 오른쪽은 거의 항상 작다. 그래도 답이 안 나오면 다음 질문:

> **"이걸 지우면 무엇이 깨지는가?"**
> 답할 수 있으면 판단할 수 있다. 답할 수 없으면 아직 지울 준비가 안 된 것이다.
> 이때는 삭제하지 말고, 모른다는 사실과 함께 사용자에게 보고한다.

## 9. 예방 — 애초에 살찌지 않기

다이어트는 이벤트가 아니라 습관이다. 새 코드를 쓸 때 다음을 지킨다.

- **YAGNI**: 지금 필요하지 않은 확장 포인트·설정·추상화를 미리 만들지 않는다.
- **3의 법칙**: 헬퍼로 추출하는 것은 세 번째 중복부터. 두 번은 그냥 두 번 쓴다.
- **로그에 수명 부여**: 디버깅용으로 넣은 출력은 그 작업이 끝나는 커밋에서 함께 제거한다.
- **작업 종료 시 회수**: 모든 작업의 마지막 단계는 "이번 작업이 만든 흔적 중
  최종 산출물에 필요 없는 것"을 되돌아보고 제거하는 것이다.
- **대체하면 원본은 삭제**: 새 구현을 넣었으면 옛 구현은 같은 커밋에서 지운다.
  둘을 동시에 남기는 순간 "무엇이 진짜인가" 비용이 영구히 발생한다.

## 10. 보고 형식

작업 후 반드시 다음 형식으로 요약한다.

### 삭제함
| 대상 | 종류 | 근거 |
|---|---|---|
| `src/utils/formatDate.ts` | 죽은 코드 | 참조 0건, 마지막 수정 8개월 전, `date-fns`로 대체됨 |

### 확인 필요 (진행하지 않음)
| 대상 | 삭제하려는 이유 | 걸리는 점 |
|---|---|---|
| `src/legacy/exportCsv.ts` | 코드 내 참조 0건 | 관리자 화면에서 문자열 라우팅으로 호출될 가능성 |

### 보류
- (판단 불가한 항목과 그 이유)

### 결과
- 삭제: N개 파일 / 약 N줄
- 검증: 빌드 ✅ / 타입체크 ✅ / 테스트 N개 통과 ✅
- 동작 변경: 없음
- 예상 효과: 전체 코드 읽기 시 컨텍스트 약 N% 감소
```
